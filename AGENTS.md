# STAX Project Instructions

## Development Focus

- Current focus is Backend and Database development.
- Do not work ahead of my instructions.
- Only implement tasks that I explicitly request.
- After completing the requested task, stop and wait for my next instruction.

## Existing Frontend

- Preserve the existing Frontend design.
- Do not redesign the UI.
- Do not significantly change existing pages, layouts, components, or styles.
- If Backend integration requires Frontend changes, make only the minimum necessary changes.
- Do not rewrite working Frontend code without a clear reason.

## Existing Project

Before implementing a task:

1. Inspect the relevant existing files.
2. Check the existing project structure and technology stack.
3. Reuse existing libraries and architecture whenever possible.
4. Do not replace frameworks/libraries unless explicitly requested.
5. Do not modify unrelated files.

## Backend and Database

- Implement only the Backend/Database feature currently requested.
- Inspect the existing database schema before changing it.
- Do not create unrelated tables, endpoints, services, or features.
- Never store passwords in plain text.
- Never expose secrets or environment variables.

## Current Known Database Requirement

users:

- ID: UUID
- Email: Unique
- Password_Hash
- Role

## Task Workflow

For each task:

1. Read AGENTS.md.
2. Inspect relevant existing project files.
3. Determine the smallest changes required.
4. Implement only the requested scope.
5. Check for errors.
6. Briefly report which files were changed and what was done.
7. Update Current Status when appropriate.
8. Stop and wait for my next instruction.

## Current Status

Project:
STAX

Current focus:
Backend and Database

Known requirement:
users table with UUID ID, unique Email, Password_Hash, and Role.

Completed:

- R13 corporate-action-aware history + R17 delete/reimport deterministic reconcile FINALIZED + fully verified (2026-09-18, uncommitted, work started at 73da3cf). Objective: ONE corporate-action-aware replay across every consumer plus numerically reproducible delete/reimport. R13: `app/lib/corporate-action.ts` RENAME collision now deterministically MERGES qty/cumQuantity/cumCost into the destination (`avgCost = cumCost/cumQuantity`, legacy fallback `existing.avgCost` when `cumQuantity <= 0`, throw on empty `newSymbol`, no-op same symbol — never overwrites destination basis); SPIN_OFF FMV `toChild = parentCumCost × childTotal/grandTotal` safe-by-construction (locked by regressions), legacy no-FMV spin unchanged. `app/lib/trading-journal-engine.ts` `buildTradingJournalEntries`/`buildBehaviorStats` now take `actions: CorporateActionInput[] = []` and interleave actions into the SAME chronological replay as `recomputeCostBasisMap` (`mergeJournalReplayEvents`: date asc `localeCompare`, same-date TRADES BEFORE actions, stable), so `avgCostAtTime` and vintage holding periods agree with cost_basis_state after splits/renames/spin-offs. Fixed a real engine bug the G regressions exposed: `applyCorporateAction` is PURE (returns a NEW map) but the replay discarded the result — actions were never applied (`let map` + `map = applyCorporateAction(...)`). Replay now applies each action via `applyActionOrSkip` (try/catch skip, documented) so a malformed legacy/hand-inserted row (e.g. SPLIT without ratioNew — exactly what the isolation fixture plants for user B) degrades to trades-only for that symbol instead of crashing a user-facing GET; write paths still reject via the SSOT validator. Route `app/routes/api/trading-journal.ts` loads corporate actions (try/catch -> []) and passes them with `from/to`/behavior scoping. R17: `app/lib/ledger-service.ts` `reconcileStatementState` (full deterministic rebuild: recompute ALL affected symbols + delete residual SKIPPED entries + re-post, own transaction, best-effort try/catch + console.error) is now the fresh+rebuild path in `statements/upload.ts` (via `affectedSymbolsOf`), so delete + re-upload reproduces the CLEAN state exactly (vs the old incremental-accumulator drift); manual rows never touched. Regressions (pure): `test-statement-tax-recon.mts` A–F +8 (SPLIT/reverse/rename-move/rename-collapse/SELL-after-merge uses merged avg/FMV) -> 87 PASS; `test-trading-journal.mts` G +9 (split avg 5.0, same-date trades-before-actions, rename inherits avg, vintage scale, malformed-SPLIT skip) -> 54 PASS; `test-ui-placeholders.mts` updated route-source strings -> 271 PASS. DB-backed (fresh disposable PostgreSQL 17 `stax-r13regtest` on :55460, removed after; other DBs untouched): **REG: DETERMINISTIC REIMPORT 13/13 assertions** (clean SELL realized +248.50 = net 748.50 − basis 500.00 [50 sh × avg 10]; reimport reproduces qty 50 == clean AND lifetime cumQuantity 100/cumCost 1000 [Webull accumulator NOT SELL-reduced]; SELL gain native+THB == clean; MANUAL row untouched + contributes to rebuilt basis; exactly 2 POSTED trade entries across reimported A + B [VAT/fee/summary rows stay SKIPPED]) — **test:w2 = 587 PASS / 0 FAIL** on a fresh single run; earlier failures were WRONG test expectations (had asserted basis 1000/−251.50/cum 150) plus an FK cleanup crash (deleting journal_entries before their journal_entry_lines — fixed: delete lines first); the first fresh run ALSO exposed the malformed-SPLIT route crash fixed via `applyActionOrSkip`. Battery: `npm test` 20 suites EXIT 0 (1276 PASS / 0 FAIL, trading-journal 54); typecheck/build EXIT 0; `npx drizzle-kit check` OK; `git diff --check` clean (LF/CRLF notices only). Files changed: `app/lib/corporate-action.ts`, `app/lib/trading-journal-engine.ts`, `app/lib/ledger-service.ts`, `app/routes/api/statements/upload.ts`, `app/routes/api/trading-journal.ts`, `scripts/run-tests.mts`, `scripts/test-statement-tax-recon.mts`, `scripts/test-trading-journal.mts`, `scripts/test-ui-placeholders.mts`. No schema/migration/frontend changes; pre-existing mojibake comment in the corporate-action.ts header left untouched (out of scope). No commit/push.

- R9-R12 general-ledger reports finalized + verified (2026-09-18, uncommitted working tree at e927335 + edits). Continued Codex's in-progress changes with no reset/revert/commit/push. R9 account ledger: `getAccountLedger` returns `opening` (account opening balance + POSTED movements before `from`), `movement` (POSTED `from..to`, signed by the account's normal side), `closing` (opening + movement) and `normalSide`, with `selectRawLines` reading only `postingState = 'POSTED'` so SKIPPED entries have zero effect; the ledger route (GET-only) exposes all four fields. R10 trial balance: closing (as-of) balances per account (openings on the normal side), `totalsByCurrency` keeps currencies distinct (never USD 100 + THB 1000 -> 1100), `totalDebit`/`totalCredit` null when >1 currency, native `debit==credit` and THB `debit==credit` equality, `balancedThb`; `getTrialBalance` deliberately ignores `from` (as-of through `to`). R11 income statement: amount = balance on the normal side (income sign-flipped), `netIncome` = income - expense per currency and native-only for single currency, consolidated THB totals from stored `amountThb` only with null-propagating `sumMoney` (missing THB -> null, `reportingStatus` PARTIAL/UNAVAILABLE, never 0). R12 balance sheet: as-of through `to`, includes opening balances (THB openings convert 1:1; foreign openings without a rate keep THB totals null), folds current-period NI (year-to-date via `periodFrom`) as `netIncomeThb`, pushes prior unclosed earnings into equity as report-only "Prior unclosed earnings" rows, `totalAssetsThb = totalLiabilitiesThb + totalEquityThb + netIncomeThb` and `balancedThb`; SKIPPED rows filtered. Report engine DTOs now alias the pure engine types (null-safe `string | null` THB); UI tabs render "-" for null THB via shared formatters, TrialBalanceTab shows closing balances + per-currency buckets + THB-base summary, IncomeStatementTab and BalanceSheetTab accept `string | null` THB, account-ledger page reads server `opening` verbatim. Regression coverage: `scripts/report-regressions.mts` new pure R9-12 suite (18 asserts: mixed native totals null/1100, distinct buckets, consolidated THB 4500 balanced, normal-side openings, contra equity on debit side, USD 200-50=150, THB 7000-1750=5250, multicurrency null native + 6250 THB, missing-THB PARTIAL, all-missing UNAVAILABLE, 40500=5000+30000+5500, native BS net income in balance, prior unclosed earnings 31000 + NI 5500, foreign openings never fabricate zero, FX balanced in reporting base only, SKIPPED zero effect on TB/IS/BS) wired into `scripts/test-general-ledger.mts` (now 189 PASS); engine tests updated to closing-balance semantics (TB totalDebit 1000.00, cash closing 600 dr); DB-backed REG: GENERAL LEDGER WIRING asserts trial-balance balance THB 35000 via the real route. Verification: npm test all 20 suites EXIT 0 (1261 reported PASS, general-ledger 189), typecheck EXIT 0, build EXIT 0, `npx drizzle-kit check` OK, git diff --check clean (LF/CRLF notice only), fresh disposable PostgreSQL 17 container `stax-r912-regtest` migrated 0027/27 and `test:w2` 574 PASS / 0 FAIL (container removed; `stax-w2test` and other DBs untouched). No commit/push; preexisting `r912-targeted.log`/`r912-typecheck.log` left as-is. R6/R7, R2/R5/R8 and earlier R4 status entries remain accurate.

- R6/R7 currency exchange and account compatibility finalized (2026-09-18, uncommitted on backend/accounting-finalization at e927335). Confirmed isFxConversion asset transfers use exactly one received-currency cash debit and one source-currency cash credit with matching detail; only this validated shape bypasses native per-currency equality, while persisted THB debit/credit must match. THB 35000 -> USD 1000 at FX 35 posts 1020 debit USD 1000 / 1010 credit THB 35000; FX 34.50 is SKIPPED for THB imbalance. Default account-code/currency policy is strict, without new accounts or schema changes: unsupported THB equity/trade/fee/income entries stay SKIPPED with reason and zero lines. Shared account resolution enforces currency on statement/manual/reversal paths; manual sync resolves UUID accounts before atomically replacing lines. FX capital rows use FX_CONVERSION and journal cash-flow type stays NULL; cash summary and backfills keep exchanges separate from deposits/withdrawals. Exchange aggregation uses Decimal without Number intermediaries; reversals preserve the FX marker, swapped source/received detail and account compatibility. Regression coverage includes real parser, persisted exchange/rebuild, manual insert/update, both mismatch directions, reversal, and cash-summary exclusion. Existing isolation fixture now uses compatible USD cash/equity accounts with an explicit test FX=1. Verified: npm test all 20 suites EXIT 0 (1280 reported assertions, no failures), typecheck/build EXIT 0, drizzle-kit check OK, git diff --check clean; fresh PostgreSQL 17 migrated through 0027, test:w2 574 PASS / 0 FAIL (24 R6/R7 DB assertions). Earlier DB runs exposed fixture assumptions corrected before the final fresh run. Frontend, Webull Average Cost, R2/R3/R4/R5/R8 calculations, reports and corporate actions unchanged. No commit/push; pre-existing r6-r7-wip.patch preserved.

- R2/R5/R8 accounting finalization (2026-09-18, uncommitted on backend/accounting-finalization). Shared accounting-amounts.ts applies decimal half-up round2 and two-stage THB conversion; parser/import, FX fallback, replay/backfill, deletion reconciliation and recompute use the same gain formula (net proceeds minus average-cost basis). Zero-gain SELL posts cash/investment only; missing-basis SELL stays visible as SKIPPED with null gains, explicit reason and no lines. Journal validator derives line/header THB server-side, validates persisted native cents, and requires same-currency entries to balance in both native and THB cents; rounding mismatches are rejected/SKIPPED rather than adjusted. Recompute clears unsupported old basis/gains and preserves skipped journal records. Added real-parser, pure journal, DB persistence, actual recompute-command and deletion regressions. Verification: npm test all 20 suites EXIT 0; typecheck/build EXIT 0; drizzle-kit check OK; git diff --check clean; fresh PostgreSQL 17 migrated through 0027, test:w2 550 PASS / 0 FAIL. Earlier DB attempts exposed test-query syntax/type/assertion defects (fixed) and a pre-existing random test-IP collision; the final fresh run passed. Webull Average Cost engine, frontend, schema, R3/R4 behavior and deferred R6/R7/R9-R17 unchanged. No commit or push.

- R4 tri-state monthly-fee provenance FINALIZED + DB-regression fixes (2026-09-17, same uncommitted scope). **(Issue 1)** migration `drizzle/0027_add_monthly_fee_aggregate.sql` REWRITTEN as a nullable tri-state column: `ADD COLUMN "is_monthly_fee_aggregate" boolean` (NO NOT NULL, NO default) on BOTH `Capital_Transactions` and `journal_entries` — so every pre-existing row is NULL (unknown), never a fabricated FALSE; comments are plain ASCII English only; documents TRUE/FALSE/NULL semantics, NO backfill (any classification would fabricate/misclassify provenance), and delete+re-import as the ONLY safe way to re-stamp old statements. `app/db/schema.ts` both columns now `boolean(...)` nullable (mirrors drop the old `.notNull().default(false)`); comments aligned to tri-state. **(Issue 2)** tri-state (boolean|null, NULL NEVER coerced to false) propagated through every R4 path: `ValidatedCapitalRow.isMonthlyFeeAggregate?: boolean | null` and write `row == null ? null : === true` (statement-pipeline.ts); `JournalTradeDetail.isMonthlyFeeAggregate: boolean | null` + `validateJournalEntry` detail coercion `d.isMonthlyFeeAggregate == null ? null : Boolean(...)` (general-ledger.ts; `emptyTradeDetail` keeps false for manual entries); `journalDetailOf` tri-state preserve (posting-engine.ts); `insertStatementImport` write + `PersistedJournalEntry`/`RawLineWithEntry` detail types (ledger-service.ts, 1048/181/1261); `CapitalJournalRecord.isMonthlyFeeAggregate: boolean | null` (journal-ledger-read.ts); DTO `?: boolean | null` (server-api.ts 407 — no *.tsx consumer exists); backfill-journal-detail.mts (`CapitalRowLike`/`DetailPatch` widened + idempotency compare now null-aware so null-vs-null is a no-op re-run); backfill-journal-ssot.mts + recompute-cost-basis-webull.mts `LedgerRow` widened. Posting behavior unchanged and documented as the deliberate NULL default: skip keys STRICTLY on `=== true` (TRUE -> SKIPPED line-less; FALSE -> standalone posted once; NULL -> historical R4 GAP-2 default posted once, stored value stays NULL so provenance stays visibly unknown). Standalone rebate still posts Dr 1020 2.00 / Cr 5010 2.00. **(Issue 3)** `scripts/run-tests.mts` REG-monthly-fee-agg block FIXED and finally EXECUTED against a REAL database: the gate `information_schema.columns` queried lowercase `table_name = 'capital_transactions'` while Drizzle created `"Capital_Transactions"` — the block had NEVER run on a real DB and silently SKIPped; gate now `lower(table_name) = 'capital_transactions'`. The legacy `is_monthly_fee_aggregate AND posting_state = 'POSTED' AS ok` assert (false AND ... = always false, would have guaranteed FAIL) replaced by INDEPENDENT field selects (`SELECT is_monthly_fee_aggregate, posting_state ...` asserted separately); `toCapitalShape` conversion now tri-state (`?? null ? null : === true`) instead of fabricating false for NULL; added a THIRD legacy row (`isMonthlyFeeAggregate: null`) asserting the capital row stores NULL, the journal row keeps flag NULL + POSTED (independent fields), and the rebuild posts it once (Dr 5010 2.50) with detail still NULL; block now also inserts its owned `documents` row (assertOwnedReferences demands it) and self-cleans it (the old block would have crashed on "Referenced resource not found"). **(Issue 4)** `scripts/test-general-ledger.mts` +5 tri-state pure asserts (R4-P5 x4: journalDetailOf preserves null, validateJournalEntry round-trips null, rebuild from a NULL legacy row posts once with flag STILL null, skip keys ONLY on TRUE so a NULL row is not treated as an aggregate; + mapper passes NULL through verbatim) -> **153 PASS / 0 FAIL** (was 148). VERIFIED ON DISPOSABLE PostgreSQL 17 (container `stax-mfa-regtest`, localhost:55443, REMOVED after; `stax-w2test` untouched): db:migrate 0000->0027 27/27, `information_schema` shows both columns NULLABLE with NO default; **`test:w2` = 542 PASS / 0 FAIL on a FRESH single run** — all 10 REG-mfa asserts EXECUTE and pass (import orders 3 rows; capital true/false/null; journal true+SKIPPED, false+POSTED, null+POSTED via independent fields; rebuild keeps monthly SKIPPED zero-lines, standalone Dr 5010 9.00 once, legacy NULL posts once with detail null). NOTE: repeated `test:w2` runs on the SAME DB inside the 15-min auth window fail AUTH legit-success asserts (residual rate-limit counters not cleaned before the new window) — fresh DB per run is the correct methodology; only the baseline run is authoritative. Non-DB battery: `npm test` all 18 suites FAIL: 0 (general-ledger 153, trading-journal 45, ui-placeholders 271, tax-recon 72, stock-prices 51, webull-parser 33, cash-summary 32, etc.), `npm run typecheck` EXIT 0, `npm run build` EXIT 0, `npx drizzle-kit check` OK, `git diff --check` clean (CRLF notice only). Webull Average Cost engine, R5 (THB posting geometry) and R7 untouched; no frontend changes; no commit/push.

- R4 final cleanup: migration 0027 comment made honest + standalone fee/rebate sign preserved (2026-09-17, same uncommitted scope). **(Issue 1)** `drizzle/0027_add_monthly_fee_aggregate.sql` comment no longer claims historical flags are "100% safe": it now documents that post-0027 imports carry the parser's deterministic flag, historical rows have UNKNOWN provenance (pre-0027 parser emitted identical persisted shapes for aggregates AND genuine standalone fees — category expense/section ค่าธรรมเนียม/VAT/type CASH_OUT/trade-fields NULL, no description column = no marker), there is NO backfill (any classification would fabricate provenance), deterministic rebuild behavior (flag=true -> always SKIPPED, flag=false -> posted once = R4 GAP-2 semantics), and delete+re-import as the ONLY safe way to get the correct flag on old statements. `app/db/schema.ts` `isMonthlyFeeAggregate` comment aligned accordingly. Verified no persisted legacy marker exists. **(Issue 2)** standalone negative fee/rebate sign preserved: `app/lib/statement-pipeline.ts` `mapToCapitalRow` no longer `Math.abs`es expense amounts — expense rows keep the parser's sign (`amountForeign`/`amountThb` signed, direction derived from the sign: positive expense -> CASH_OUT, negative rebate -> CASH_IN); non-expense rows keep the historical positive-magnitude convention. `app/lib/posting-engine.ts` expense branch keys on amount SIGN (not type): negative = broker rebate -> posts the contra-expense Dr cash |amt| / Cr fee |amt| with POSITIVE leg amounts (validateJournalEntry + `chk_journal_entry_lines_debit/credit_positive` safe), never a phoney expense debit; positive stays Dr fee / Cr cash; the `isMonthlyFeeAggregate` skip unchanged. Header comment documents the rebate case. Tests in `scripts/test-general-ledger.mts`: R4-G2b (postCapitalRow rebate -2 -> Dr 1020 2.00 / Cr 5010 2.00, no 5010 debit/cash credit, positive legs), R4-Fb (full buildStatementJournalEntries path accepts rebate -3.50 POSTED via validateJournalEntry), R4-G2c (mapToCapitalRow: expense rebate -2 -> "-2.00"/THB "-70.00"/CASH_IN; fee +9 -> 9.00/315.00/CASH_OUT; equity +1000 -> 1000.00/CASH_IN) -> **general-ledger 148 PASS / 0 FAIL**. Battery re-run: npm test 18 suites all FAIL 0 (~1248 PASS, EXIT 0), typecheck EXIT 0, build EXIT 0, `npx drizzle-kit check` OK, `git diff --check` clean. Repo-wide mojibake grep still clean. NOT committed/pushed; `stax-w2test` untouched. R4 Gauss scope addressed by the migration-comment honesty: the flag now survives persistence, but by design historical rows keep the legacy default-false treatment and the only correction path is documented re-import — no silent reclassification.

- R4 FEES accounting finalized (2026-09-17, branch `backend/accounting-finalization`, working tree at HEAD 35c731b + uncommitted edits). All 20 npm test suites, typecheck, build, drizzle-kit check and `git diff --check` re-verified clean after the ledger-service repair below (all suites FAIL: 0, npm test EXIT 0). Implemented all 5 remaining R4 gaps: (1) **BUY principal = Decimal(qty × unitPrice)**, fees excluded — `postCapitalRow` BUY branch (`app/lib/posting-engine.ts`) now derives the investment debit from `quantity × unitPrice` and NEVER falls back to capitalizing the net amount: a BUY row without a parseable positive principal returns `ok:false` (SKIPPED journal entry, zero lines) instead of posting net as the asset; (2) **deterministic monthly-aggregate provenance** — new `isMonthlyFeeAggregate` flag on `ExtractedTransaction` (parser Pass 3) → `ValidatedCapitalRow` → **persisted as a real boolean column (`is_monthly_fee_aggregate`, migration 0027) on both `Capital_Transactions` and `journal_entries`** so it survives persistence and the rebuild paths (deletion reconciliation, recompute/backfill scripts) → the expense-branch skip now keys on the flag instead of a side-effect broad `section === "ค่าธรรมเนียม"/"VAT"` match, so a genuine standalone broker fee (same section label, no flag) still posts Dr 5010 once; THB trade assertion kept at parser/pipeline currency separation (THB posting geometry explicitly pending R7 — CoA defines 1110/5010 as USD, so no THB leg layout is asserted); (3) **per-currency fee/VAT aggregation** — parser replaced scalars `feeTotal`/`vatTotal`/`tradeCurrency` with `feeTotals`/`vatTotals` maps keyed by currency and emits one fee row + one VAT row PER currency (USD fee 1 + THB fee 10 stay two rows, never one "11" in a single currency); (4) **SELL fee policy documented** in the posting-engine header: `realizedGainLoss = net proceeds − costBasis`, the SELL fee is already netted inside the proceeds and is NEVER subtracted again nor posted as a separate expense line; (5) **real parser→pipeline→posting regression** in `scripts/test-general-ledger.mts` (R4-G1..G5): statement text → `parseStatementRows` → `mapToCapitalRow` → `postCapitalRow`/`buildStatementJournalEntries`, asserting mixed USD+THB BUY mapping (principal/fee/net/fx=1), per-currency monthly rows both SKIPPED, USD BUY 1000/1/1001 via 1020, THB BUY mapped at parser/pipeline (qty/price/fee/net, fx pinned 1 — posting geometry pending R7, not asserted), SELL computed net 1189/basis 1000/gain 189 through the FULL parser path with no 5010 leg. R4-D/R4-F fixtures updated to set `isMonthlyFeeAggregate: true`. After the 5 gaps, `app/lib/ledger-service.ts` was repaired so the flag actually PERSISTS: all 4 journal-insert `.values` blocks (createJournalEntry / insertManualCashJournal / insertBackfilledJournalEntry / insertStatementImport) + `syncCapitalLedgerJournal` `.set` + `manualCashDetail` write `isMonthlyFeeAggregate` (corrected a prior iterative-edit error that had corrupted the file's UTF-8 Thai text with an ASCII rewrite; no code damage remained after restore+clean re-patch) and the read layer (`RawLineWithEntry.detail`, `selectRawLines`, `toPersistedEntry`, `fetchJournalHeaders`) carries it, with `type` columns verified against HEAD (createJournalEntry none; manualCash/backfilled `input.type`; import `plan.row.type`). Battery (rerun 2026-09-17): typecheck EXIT 0; test-general-ledger **141 PASS / 0 FAIL** (was 137, +R4-P1/P2/P3 persistence round-trips via journalDetailOf + CapitalJournalRecord mapping), webull-parser 33, tax-recon 72, trading-journal 45 all PASS 0 FAIL; `npm test` all 20 suites FAIL 0, EXIT 0; build EXIT 0; `npx drizzle-kit check` OK; `git diff --check` clean (CRLF notice only). Full working-tree scope (all uncommitted): `posting-engine.ts`, `pdfStatementParser.ts`, `statement-pipeline.ts`, `ledger-service.ts`, `general-ledger.ts`, `journal-ledger-read.ts`, `server-api.ts`, `schema.ts`, `drizzle/0027_add_monthly_fee_aggregate.sql`, `drizzle/meta/_journal.json`, `scripts/backfill-journal-detail.mts`, `scripts/backfill-journal-ssot.mts`, `scripts/recompute-cost-basis-webull.mts`, `scripts/run-tests.mts`, `scripts/test-general-ledger.mts`, `scripts/test-trading-journal.mts`; no frontend changes; Webull Average Cost engine untouched. NOT committed/pushed; `stax-w2test` untouched.

- Statement deletion release blocker fixed (2026-09-17, `backend/final-system-check`): statement PDF deletion now fully reconciles affected journal/capital/cost-basis state in a guarded deletion transaction... (previous entries preserved below)

- R3 FX unknown-data handling fixed (2026-09-17, NOT committed/pushed): non-THB rows with no statement rate and no historical-provider rate now store `amount_thb`/`fx_rate_effective` as **NULL** instead of the previous fabricated 1:1. `statement-pipeline.ts` `mapToCapitalRow` no longer falls back to `parsedRate ?? 1` and nulls `amountThb`/`realizedGainLossThb` when the rate is unknown; `applyFxRateFallback` docs updated (unresolved -> NULL, never 1, never 0). `posting-engine.ts` `postCapitalRow` refuses to post a non-THB row without a valid rate (SKIPPED journal entry, zero lines) and `leg()` no longer defaults a missing rate to "1". `app/db/schema.ts` `Capital_Transactions.amount_thb` made nullable; new migration **`drizzle/0026_allow_null_capital_amount_thb.sql`** (+ journal idx 26) drops NOT NULL - journal_entry_lines stays NOT NULL (a line is only written for a row with a valid rate). `scripts/recompute-cost-basis-webull.mts` `LedgerRow.amountThb` widened to `string | null`. Tests: `test-statement-tax-recon.mts` regression A-E added (statement 35.42 -> 3542; provider 34.50 base-NULL then -> 3450; provider-unavailable -> NULL/never 100; THB -> 1/100; posting-engine refuses + SKIPPED zero-lines + control posts) -> 72 PASS; `test-audit-actions-sync.mts` now locates the `chk_audit_logs_action` migration **by marker** instead of assuming the numerically-latest migration. Battery: typecheck EXIT 0, build EXIT 0, npm test **1236 PASS / 0 FAIL (20 suites)**, `npx drizzle-kit check` OK, `git diff --check` clean (CRLF notice only). Docker daemon unavailable, so migration 0026 was NOT applied/verified against a live PostgreSQL (SQL is a single `ALTER TABLE ... DROP NOT NULL`; no amount_thb CHECK on Capital_Transactions, verified in 0025). No commit/push.

- Final deletion confirmation/Admin review (2026-09-17, `backend/final-system-check`, HEAD f6ec6b0): preserved all inherited working-tree changes. StatementArchivePage trash opens a native in-app Thai confirmation dialog with actual filename and financial-data warning; cancel sends zero DELETE, confirm sends one with existing InFlightDeletionGuard retained, pending controls disabled, existing errors/parent refresh preserved. Owned document query and successful STATEMENT_DELETE audit now include originalName, never filePath; deletion transaction/reconciliation/storage cleanup unchanged. Admin review passed without Admin edits (authenticated database ADMIN only, USER denied, USER-only status changes, ADMIN status protected, delete audit visible, document/stats reload reflects deletion, no Admin document-delete capability). Added component-handler regressions, 3 DB audit assertions, and 14 HTTP Admin/audit assertions. Fresh disposable PG17 25/25 migrations; all 20 npm test suites pass; test:w2 **532 PASS / 0 FAIL**; typecheck/build/Drizzle/diff-check pass; built Node HTTP smoke **81 PASS / 0 FAIL**. Browser unavailable, so visual modal QA was not run; actual component handler tests passed. No commit/push.

- Statement deletion release blocker fixed (2026-09-17, `backend/final-system-check`, initial HEAD f6ec6b0). Reproduced against built Node/isolated PG17: document/capital deletion left journal SSOT entries and lines. DELETE now locks/rechecks the owned document, deletes user-scoped document/transaction-linked journal lines then headers, capital rows and document in one transaction, and reconciles remaining cost basis plus affected imported SELL capital/journal details and posting lines using existing replay/posting engines. Reconciliation failure rolls back deletion; storage cleanup stays after commit. Manual/unrelated rows, other statements, corporate actions and USER B preserved. Added 42 DB assertions (including injected rollback, legacy CSV cascade, transaction-only mirrors/document-only headers, all financial views, exact re-import counts, and stale SELL basis/gain removal); expanded HTTP smoke to 67 PASS. Fresh PG17 25/25 migrations; test:w2 529 PASS / 0 FAIL; all 20 npm test suites, typecheck, build and drizzle-kit check pass. No frontend/schema/migration changes, commit or push. Concurrent README edit preserved. Existing orphan data from earlier deletions was not repaired automatically; documented separately in handoff.

- Final Node system check (2026-09-17, `backend/final-system-check`): started clean at f24fc4e (PRs #29/#30 integrated). Confirmed and fixed fresh corrupt/textless PDF uploads leaving metadata/storage after extraction errors by extracting before storage. Added route regressions and expanded built Node HTTP smoke to 51 checks, including real PDF preview/import/duplicate/download/delete and storage-write failure. Corrected the old corrupt telemetry PDF fixture to a valid extractable PDF. Fresh disposable PostgreSQL 17: 25/25 migrations, `test:w2` 487 PASS / 0 FAIL; all 20 npm test suites, typecheck, build and Drizzle check pass. Local storage only; external fetches blocked using the test-only preload; no production services used. Version 1.0.0 in package.json with lockfile mirror; local env defaults and docs/backend-handoff.md document setup, commands and non-blocking follow-ups. No frontend/schema/migration changes. No commit or push; awaiting review.

- Authorization / User Isolation audit completed (2026-09-16, `backend/user-isolation-audit`, backend only). Preserved the four inherited working-tree changes. Full 39-route ownership matrix and findings in `docs/user-isolation-audit.md`. No remotely exploitable cross-user API IDOR reproduced; fixed latent service reference-ownership gaps with transactional ID+owner checks and SHARE locks (`app/lib/resource-ownership.ts`), retained authoritative import userId and ledger mutation scoping, scoped settings mutation/reread, hardened aggregate joins against inconsistent owners, standardized foreign/missing account and reversal responses to 404, sanitized logs in changed runtime files. Added real two-user tests via `scripts/user-isolation-db.mts` invoked by `scripts/run-tests.mts`: all 22 requested categories covered or explicitly N/A (document/corporate-action metadata GET are 405), B database/PDF state unchanged, A=100/B=900 aggregates separate, forged account/document/transaction links rejected, same-PDF preview/import/duplicate/rebuild/delete remain per-user, USER and stale ADMIN JWT claims denied while DB ADMIN works. `npm test`: all 20 suites pass, including 260 source guards; `test:w2`: **461 PASS / 0 FAIL** (105 new isolation assertions); typecheck/build/drizzle-kit check/diff-check pass. Fresh disposable PostgreSQL 17 `stax-isolation-audit-test` on localhost:55442 migrated and verified 25/25; exact container removed after ID/label verification. Test PDFs removed, including four leftovers from existing test blocks; pre-existing storage and other databases untouched. No schema/frontend/deployment changes, commit, or push. Follow-ups documented: composite ownership FKs, legacy dangling-link repair if needed, same-user concurrent reversal atomicity, raw logging in untouched files. Awaiting review.

- P2 Data Integrity final upgrade-safety review — PR #28 decision: **14 constraints kept, 8 deferred**. Live PostgreSQL 17 testing proved the fatal flaw in NOT VALID-as-legacy-compatibility: NOT VALID skips validation only at ADD-time; **any UPDATE of any column re-checks ALL row-level CHECKs**, so a legacy row holding an out-of-domain value broke on ANY unrelated UPDATE (23514). Proof: seeded legacy rows with out-of-domain values (User role=PORTFOLIO_VIEWER status=DEACTIVATED, Capital_Transactions type=BUY source=CSV side=RIGHT category=liability qty=0, notifications type=EMAIL_ALERT, audit action=LEGACY_ACTION_X) re-applied 0025 then ran the REAL app UPDATE paths — heartbeat `UPDATE User SET last_seen_at` → 23514, login (last_login_at+last_seen_at) → 23514, admin status flip → 23514, capital ledger PUT `UPDATE amount_foreign` → 23514, notification mark-read → 23514. **DEFERRED the 8** (users role/status, capital type/source_type/side/category/quantity_positive, notifications type): removed from `drizzle/0025_add_data_integrity_checks.sql` + `app/db/schema.ts` check() mirrors; service-layer validation (the same constant sets) already blocks new invalid writes, so deferring costs nothing while guaranteeing zero legacy breakage. **KEPT 14**: 13 VALIDATED (corporate_actions action_type (4), accounts type + opening_balance_non_negative, journal_entries source_type/status/side/posting_state/type (5), journal_entry_lines debit_positive/credit_positive/fx_rate_effective_positive/amount_thb_positive (4), auth_rate_limits attempts_non_negative) + `chk_audit_logs_action` as the ONLY NOT VALID — audit_logs is append-only (zero `.update(auditLogs)` anywhere in the repo; Phase-3 backfill scripts only INSERT), so NOT VALID can never break an UPDATE. `scripts/run-tests.mts` REG: DATA INTEGRITY CHECKS reworked: gate → `chk_audit_logs_action`; asserts 14 present + 8 ABSENT (`absentChk.length === 0`); `assertTier` — audit NOT VALID, journal/accounts/lines/attempts VALIDATED; kept all 10 invalid-journal/journal-line/attempts 23514 rejects; removed the 7 invalid-write asserts for deferred columns; NEW **upgrade-regression** block inserts the same out-of-domain legacy rows and asserts the real UPDATE paths now SUCCEED (no 23514) + self-cleans. **VERIFIED live on disposable PG17**: (1) fresh `stax-w2final` (55440, removed) — db:migrate 0000→0025 25/25, ci-verify --verify OK, test:w2 **356 PASS / 0 FAIL** (all REG-di asserts incl. 14-present/8-absent/upgrade-regression); (2) upgrade sim `stax-upgsim` (55441, removed) — migrate→drop 14→seed legacy→re-apply new 0025→rows SURVIVE, 13 VALIDATED + audit NOT VALID confirmed from `pg_constraint.convalidated`, all 5 UPDATE paths `UPDATE 1` (frozen NO), invalid journal status + bad audit action still 23514. Battery: drizzle-kit check OK, typecheck EXIT=0, npm test (19 suites) EXIT=0, build EXIT=0, git diff --check clean. Files changed: `drizzle/0025_add_data_integrity_checks.sql`, `app/db/schema.ts`, `scripts/run-tests.mts`, `AGENTS.md`. NOT committed/pushed; `stax-w2test` untouched.
- P2 Database Constraints / Data Integrity Hardening (approved 22-item plan, backend only, branch `backend/data-integrity`). New migration `drizzle/0025_add_data_integrity_checks.sql` (journal idx 25) adds 22 CHECK constraints in two tiers. **NOT VALID (9)** — pre-repo / removed-Webull-CSV legacy values unprovable, new writes fully enforced: `chk_users_role`, `chk_users_status`, `chk_capital_transactions_type`, `chk_capital_transactions_source_type`, `chk_capital_transactions_side`, `chk_capital_transactions_category`, `chk_capital_transactions_quantity_positive`, `chk_notifications_type` (6), `chk_audit_logs_action` (25). **VALIDATED (13)** — audited repo-only writers: `chk_corporate_actions_action_type` (4), `chk_accounts_type` (5), `chk_accounts_opening_balance_non_negative`, `chk_journal_entries_source_type`, `chk_journal_entries_status`, `chk_journal_entries_side`, `chk_journal_entries_posting_state`, `chk_journal_entries_type`, `chk_journal_entry_lines_debit_positive`, `chk_journal_entry_lines_credit_positive`, `chk_journal_entry_lines_fx_rate_effective_positive`, `chk_journal_entry_lines_amount_thb_positive`, `chk_auth_rate_limits_attempts_non_negative`. No ENUMs; CHECK over TEXT; double-entry balance stays app-level (noted in schema); per-constraint evidence comments (file:line) in the migration; `app/db/schema.ts` mirrors every check with Drizzle `check(...)` so generate/CI never drift. `scripts/run-tests.mts` gains `REG: DATA INTEGRITY CHECKS` (gated on `chk_users_role`; ~43 asserts: all-22 presence, NOT VALID + VALIDATED tier spot-checks, `expectCheckViolation` 23514 for every constraint, valid-value sweep across all tables, self-cleaning). **VERIFIED live on disposable PostgreSQL 17** (container removed): (1) fresh DB — ci-verify 0→migrate 25/25, tier query 9f/13t registered, test:w2 **368 PASS / 0 FAIL**; (2) upgrade 0024→0025 simulation — dropped the 22, seeded legacy rows incl. values outside the NOT VALID sets (role PORTFOLIO_VIEWER, status DEACTIVATED, type BUY/source CSV/side RIGHT/category liability/qty 0, notification EMAIL_ALERT, audit LEGACY_ACTION_X) + clean VALIDATED values, re-applied 0025 → **22 ALTERs succeeded**, all legacy rows survived, informational `VALIDATE chk_users_role` correctly FAILs (justifying NOT VALID), new invalid writes rejected on BOTH tiers (23514); (3) skip path — dropping `chk_users_role` makes the REG block **SKIP cleanly**, test:w2 325 PASS / 0 FAIL. npm test 18 suites FAIL: 0; typecheck + build EXIT 0; npx drizzle-kit check OK; git diff --check clean; scope = exactly 4 files (0025 SQL, journal, schema.ts, run-tests.mts). NOT committed/pushed; `stax-w2test` untouched.
- P2 Auth Hardening PR #27 review fixes applied + fully verified live on a fresh disposable PostgreSQL 17 (backend only, branch `backend/auth-hardening`). (1) Register budget now truly **per-IP**: `registerIpKey(ip)` → `register-ip:<ip>` (email REMOVED from the key — a per-IP-per-email key let an attacker rotate emails from one IP and get a fresh bucket each time; register.ts pre-check + 23505 race still share the one IP budget → 11th well-formed register from an IP is 429 before bcrypt/INSERT). (2) Successful login no longer wipes the shared IP bucket: new `rollbackRateLimit(key)` in rate-limit.ts (atomic `UPDATE ... SET attempts = attempts - 1 WHERE key=$1 AND attempts >= 2`, and only when the RETURNING update matched 0 rows does a `DELETE WHERE attempts <= 1` fire — concurrent-safe, never negative, fail-open); login.ts success now `Promise.all([rollbackRateLimit(ipKey), clearRateLimit(emailKey)])` — the email bucket resets but PRIOR per-IP spray failures are preserved. (3) Comments corrected: evaluateRateLimit doc now states `attempts` is post-increment INCLUDING the current request (attempts===max is the max-th allowed request; the 6th/21st/11th → 429); "per-IP-per-email"/"clears both keys" wording removed. DB-backed regression scenarios added to run-tests.mts REG: AUTH HARDENING (#7 rotating emails same IP: 10×201 / 11th 429 / fresh IP 201; #8 IP-reset bypass: 19 failures + ONE success → failure #20 401, #21 429 — prior spray never forgiven; #9 legit: 6 successes → DB check `auth_rate_limits` login-ip row absent (net 0), post-success email bucket fully reset → 6th failure 429). Harness now registers via `registerAs()` (random x-forwarded-for per call) at every OTHER REG setup site (smoke/created_at/GL/TXREC/pf/backfill/webull/notes/spin/scope) so the per-IP register budget is never exhausted by the suite's own account creation — only the intentional-spam tests pin a shared IP. test-rate-limit.mts updated (registerIpKey IP-only + "never embeds an email" + boundary wording; 44 PASS). VERIFIED live: fresh container `stax-auth-review-test` (postgres:17-alpine, localhost:55437, `stax_auth_review_test`): db:migrate through 0024 clean (`\d auth_rate_limits` matches schema), test:w2 **325 PASS / 0 FAIL** (all 9 AUTH scenarios incl. the 3 new DB-backed ones), npm test 17 DB-free suites FAIL: 0 (rate-limit 44), typecheck + build + npx drizzle-kit check + git diff --check clean (CRLF notices only). git status shows ONLY the 6 intended files (rate-limit-core/rate-limit/login/register/run-tests/test-rate-limit). Container removed; `stax-w2test` untouched. Migration 0024 UNCHANGED. NOT committed, NOT pushed — awaiting review.
- P2 Auth Hardening implemented and fully verified live on a disposable PostgreSQL 17 container (backend only; branch `backend/auth-hardening`). RESOLVED during the live run: (1) rate-limit counter undercount — the fresh-INSERT path seeded `attempts=0` so after 6 failures the counter read 5 (≤ max) and 429 never fired; `incrementRateLimit` now inserts `attempts: 1` so post-increment semantics match the pure `evaluateRateLimit` (5 failures → 401, 6th → 429, IP cap 20 → 429 on 21st, register 10 → 429 on 11th, clear-on-success resets both keys); (2) 23505 detection — drizzle postgres-js wraps the postgres error in `DrizzleQueryError` with the original on `.cause`, so `error.code === "23505"` never matched and concurrent-register losers returned 500; register.ts now checks `error.code` OR `error.cause?.code`, race → 409 EMAIL_ALREADY_EXISTS with audit, never 500. Final layout: `drizzle/0024_add_auth_rate_limits.sql` + journal idx 24 (auth_rate_limits: key PK, attempts default 0, window_started_at/updated_at timestamptz default now(), updated_at index); `app/lib/rate-limit-core.ts` (pure: policies LOGIN_EMAIL 5 / LOGIN_IP 20 / REGISTER_IP 10 per 15-min window, key builders, clientIpFromRequest x-forwarded-for→x-real-ip→cf-connecting-ip with charset/64-char guard fallback "unknown", post-increment evaluateRateLimit, rateLimitResponse); `app/lib/rate-limit.ts` (re-exports core + DB helpers incrementRateLimit/clearRateLimit/purgeStaleRateLimits — atomic ON CONFLICT upsert with make_interval window reset, probabilistic purge 1/50 >24h, all fail-open never 500); login.ts increments BOTH login-ip:<ip> and login-email:<email> FIRST before bcrypt (resource DoS), 429 generic body + Retry-After before any bcrypt; register.ts increments the per-IP register bucket after validation, pre-check + 23505 both land on the same budget → duplicates 409 not 429; `app/lib/safe-error-log.ts` sanitizes all auth console.error (null→"null", non-object→typeof, objects→sanitized constructor.name); audit-log.ts + auth-middleware.ts catches sanitized. Verified: disposable container localhost:55436 (f084d40dfa7f… REMOVED after; `stax-w2test` untouched): db:migrate applied 0024, table schema confirmed, call-by-call boundary debug confirmed 1..5 allowed/6th limited per key, full `run-tests.mts` **276 PASS / 0 FAIL** (all 62 AUTH: scenarios incl. concurrent-register one-201/three-409/never-500, 6th-login 429+code+Retry-After, IP-cap 21st 429 generic-body no-leak, clear-resets-budget, register spam 11th 429, identical unknown-vs-wrong-password bodies). DB-free `npm test` all 17 suites FAIL: 0 (test-rate-limit.mts 41 PASS wired into npm test); `npx drizzle-kit check` fine; typecheck + build EXIT 0; git diff --check clean (CRLF notices only). NOT committed/pushed; no production/dev DB touched.
- Authentication email normalization unified (2026-09-16, Backend only): new `app/lib/normalize-email.ts` applies trim + lowercase; both `app/routes/api/auth/register.ts` and `login.ts` call it (Register already lowercased; Login previously only trimmed). Password hashing/comparison, JWT, roles and session logic unchanged; no schema/data migration or Frontend changes. Added `scripts/test-auth-email.mts` to npm test: 6 DB-free checks plus opt-in `--database` real-route regression guarded by local TEST_DATABASE_URL and a database name ending in _test. Verified all 18 checks against a newly created PostgreSQL 17 container `stax-auth-email-test` on localhost:55435 after db:migrate: canonical stored email, mixed-case/whitespace login, duplicate normalization, wrong email/password 401, password case/whitespace significance, invalid email 400. Removed only the task-created container after checking its ID; no production/old database used. npm run typecheck, npm test (17 suites), npm run build and git diff --check passed. NOT committed or pushed.
- Vercel stock-price cron hardened (2026-09-16, Backend only): inspected existing GET support and Yahoo provider; kept provider/service unchanged. `app/routes/api/stock-prices/refresh.ts` now delegates to the DB-free-testable `app/lib/stock-price-refresh-handler.server.ts`: GET accepts matching Bearer CRON_SECRET (legacy x-cron-secret preserved), missing/wrong/unconfigured secret -> 401 without refresh or JWT fallback; only GET/POST allowed (other methods -> 405 + Allow); existing ADMIN JWT POST preserved; constant-time equal-length secret comparison, no-store responses and sanitized refresh failure. `vercel.json` moved daily schedule from 17:30 to 22:30 UTC (05:30 ICT; after the regular US close in EST/EDT); `.env.example` has a blank CRON_SECRET placeholder and Production setup guidance. Added 29 handler/config/DST assertions to `scripts/test-stock-prices.mts` (51 PASS), updated the schedule assertion in `scripts/test-ui-placeholders.mts`, and added real GET/Bearer cases plus revised 401 expectation in `scripts/run-tests.mts`. npm test all 16 suites passed, typecheck/build/git diff --check passed. DB-backed test:w2 additions NOT run; no production DB connection or live Vercel invocation. Set CRON_SECRET in Vercel Production and deploy to activate; existing deployment adapter setup remains separate. No Frontend/schema/provider changes. NOT committed or pushed.
- Empty PostgreSQL migration verification completed (2026-09-16): created only temporary container `stax-migration-test` (PostgreSQL 17.11, localhost:55434, tmpfs data), confirmed 0 initial user tables, then ran `npm run db:migrate` successfully through 0023 (0018 remains absent). Verified all 23 rows in drizzle.__drizzle_migrations against local SQL hashes and journal timestamps; verified all 65 columns introduced by 0014-0023, types/nullability and relevant defaults, 10 indexes/uniqueness, 3 primary keys and 3 foreign keys including CSV document-delete cascade. No migration/journal correction needed. Re-ran typecheck, npm test (all 16 suites), build and git diff --check successfully. All database URLs were process-local and pointed only to the new test container; no production/.env database connection. Removed the task-created container after verifying its exact ID and confirmed removal; `stax-w2test` was not used, started, inspected or removed. Only this status documentation was changed in this follow-up; no Frontend/schema/SQL changes, commit or push.
- Drizzle migration journal repaired (2026-09-16, Backend/Database only): reviewed every existing SQL migration 0014-0023 and registered the 9 missing files (0014-0017, 0019-0023) in drizzle/meta/_journal.json. Preserved entries 0000-0013; appended sequential journal indices and strictly increasing registration timestamps. Migration 0018 remains absent (previously reverted); no SQL, schema, or Frontend changes. Verified all 23 SQL files match the journal and load through the installed Drizzle migration reader. npm install completed without package/lockfile changes; npm run typecheck, npm test (DB-free suites with a process-local placeholder DATABASE_URL), npm run build, and git diff --check passed. Empty PostgreSQL db:migrate test NOT run: no TEST_DATABASE_URL/.env or local PostgreSQL tools; Docker daemon unavailable. NOT committed or pushed.
- Existing project cloned from the current shared repository.
- React Router development skill exists under .agents/skills/react-router/.
- Prisma 7 configured with SQLite (prisma.config.ts, prisma/schema.prisma, .env).
- users table created via migration create_users (prisma/migrations/20260807130407_create_users).
- Prisma Client generated under prisma/generated/prisma.
- documents table created via migration create_documents (prisma/migrations/20260808105847_create_documents). Stores metadata + file_path for uploaded Statement PDFs (no binary in DB). FK documents.user_id -> User.id with index on user_id.
- Server-authoritative frontend wiring done (Drizzle + Postgres/Supabase; Prisma is legacy, not used): added user-scoped `GET /api/v1/documents` (app/routes/api/documents.ts) returning safe metadata; shared `app/lib/server-api.ts` (fetchCapitalLedger / fetchUserDocuments / capitalRowToTransaction / capitalLedgerToTransactions — identity=authoritative transactionId, no fabricated P&L). Dashboard, FxAiPage, Calendar, Statement Archive, and StoredDocumentsList now render from server (ledger + documents API) instead of session-import React state / IndexedDB. Tests: scripts/test-server-api.mts (pure mapping + wiring) added to `npm test`; W2-9 documents user-scoping section added to scripts/run-tests.mts. All tests + typecheck + build pass. Changes NOT committed.
- Server-authoritative Statement download done: `GET /api/v1/documents/:id/download` (app/routes/api/documents.$id.download.ts, registered in app/routes.ts) streams the stored PDF for the authenticated user's OWN document only. Safe 404 on cross-user/missing; file_path resolved strictly inside STATEMENTS_DIR (pure helpers in app/lib/storage/statement-path.ts, DB-free); missing physical file -> safe 404/410 with sanitized server log, no metadata rewrite; Content-Disposition filename sanitized (no header injection); body streamed via Readable.toWeb (not buffered). Frontend StatementArchivePage + StoredDocumentsList download via `downloadUserDocument` (server Blob) — IndexedDB no longer authoritative. Regression coverage: DB-back tests added in scripts/run-tests.mts (DL section, runs only under test:w2 with TEST_DATABASE_URL) + DB-free tests in scripts/test-document-download.mts added to `npm test`. npm test 218 PASS/0 FAIL; typecheck + build pass; git diff --check clean (benign CRLF notice). Changes NOT committed; on branch fix/server-authoritative-statement-download.
- Storage abstraction done (filesystem -> private Supabase Storage ready): new `app/lib/storage/storage-driver.ts` (StatementStorageDriver interface, object keys `statements/<userId>/<documentId>.pdf`, localStorageDriver + supabaseStorageDriver, STORAGE_MODE=supabase|local or auto-detect from SUPABASE_* env vars; tests default to local so they never touch live Supabase). Upload/download/delete now go through getStorageDriver(); statement-storage.ts saveStatementPdf generates documentId before storage and persists the object key in documents.file_path; upload extracts text from in-memory bytes and does best-effort object + DB row cleanup on processing failure; @supabase/supabase-js@2.112.4 installed; .env.example gained SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY / SUPABASE_STORAGE_BUCKET / STORAGE_MODE placeholders. W2-10 stale test fixed (suspended login + forged-JWT both rejected). DB-backed test:w2 suite now 102 PASS/0 FAIL (adds: upload MIME/magic bytes/size validation, duplicate-upload rejection, register->login->session smoke); npm test 236 PASS/0 FAIL; typecheck + build pass; git diff --check clean. NOT committed; NOT deployed — Vercel preview deploy blocked on human config (vercel CLI login, project link, @react-router/vercel preset or vercel.json, and setting runtime env vars in the dashboard).
- Production audit done: no VITE_* secrets, no dev.db/SQLite runtime deps (Prisma legacy), JWT_SECRET/DATABASE_URL/GEMINI_API_KEY/GEMINI_MODEL server-only, verifyAuth is DB-authoritative (suspended enforced), app routes use relative /api/v1 paths, SSR true without a Vercel preset (deployment adapter decision pending).
- Dashboard financial/tax summary made server-authoritative and honest: fixed the double FX conversion (pnlAmount is already realizedGainLossThb in THB, no `pnlAmount * rate` anywhere); Net Realized P&L card now sums authoritative THB values and only warns ("มีบางรายการที่ยังคำนวณไม่ได้") instead of blanking the whole card when some rows are null; Exchange-rate card derives the ACTUAL rate used — fxRateStatement (Statement) first, external historical provider only as fallback, THB = 1 — with source label and per-transaction caveat, no "BOT API failure" wording; Tax card now calls POST /api/v1/tax/calculate and shows the authoritative tax base ("ฐานภาษีที่คำนวณได้"), labeled as tax base not tax payable, keeps NOT AVAILABLE only when genuinely nothing is computable. Tax Core table reworked on a new server `classification` field: realized-gain / buy-basis / non-computable / not-applicable (CASH/BUY no longer shown as REALIZED_GAIN_LOSS_NOT_COMPUTABLE) and now renders trade detail (symbol, side, qty, price, gross, fees, proceeds, costBasis, realizedGainLoss, fxRateEffective, realizedGainLossThb). Calendar fixed the same double conversion. API DTOs extended (capital-ledgers raw rows + tax/calculate rows) with all trade fields for server-authoritative rendering; no P&L/tax computation in React. Verified live on dev data: 73 rows, Tax Core total ฿140.45 (NVDA SELL computable; 40 BUY=buy-basis, 2 SELL=non-computable, 30 cash=not-applicable). Tests updated (test-server-api +7, test-statement-tax-recon +6, test-ui-placeholders +6). npm test 323 PASS/0 FAIL; test:w2 125 PASS/0 FAIL; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.
- Re-import-after-deletion bug FIXED (confirmed root cause: upload step 0 did hash-only duplicate detection via findExistingDocumentByHash and early-returned the duplicate payload BEFORE checking hasSavedDocumentRows; when a user deleted their financial/ledger data — per-row ledger delete or manual DB delete leaves the documents row + stored PDF + content_hash intact — re-uploading the same PDF was rejected as a duplicate and the pipeline never ran, so zero rows were restored; count only). Fixed `app/routes/api/statements/upload.ts`: duplicate decision now checks `hasSavedDocumentRows` — rows present -> duplicate payload (unchanged); no rows -> `rebuildStatementImport` re-extracts+re-parses the uploaded bytes and re-inserts under the EXISTING document id (no new doc/storage object, no duplicate rows), saves cost basis AFTER commit, audits `result:"reimported"`, notifies import; re-parse of 0 rows / extraction failure / unsupported PDF -> stable duplicate payload (keeps W2-10 green). Response adds `rebuilt`, `duplicateDecision` ("duplicate"|"rebuilt"|"fresh"|"unsupported"), `stats` (buyCount/sellCount/cashCount/computableSellCount/statementFxCount/fxRates) — additive, frontend-safe. `app/routes/api/documents.$id.ts` + `app/routes/api/capital-ledgers.$id.ts` now best-effort `rebuildCostBasisStateFromLedger(userId)` after delete (pure replay `recomputeCostBasisMap` mirrors the parser average-cost math: BUY avg=(prevQty*prevAvg+qty*price)/(prevQty+qty), SELL deducts qty with unchanged avg, full drain removes symbol) so cost_basis_state never drifts from the remaining rows. Tests: pure replay/stats tests in scripts/test-statement-tax-recon.mts (+7), DB-backed "REG: RE-IMPORT AFTER LEDGER DELETION" block in scripts/run-tests.mts (minimal pdfjs-legacy-readable PDF builder makePdf; covers import -> dup-reject-with-rows -> simulate deletion -> rebuild under same doc id -> basis re-persist -> statement DELETE reconciles basis), test-ui-placeholders.mts ordering guard re-targeted to the fresh import path. Verified live on dev data (real stored 2026-01.PDF, Supabase DB): after wiping the user's Capital_Transactions + cost_basis_state (document stayed), re-upload returned `rebuilt:true, duplicateDecision:"rebuilt", documentId unchanged 1511e700...`, extracted 69 / saved 59 (36 BUY, 11 SELL, 12 cash, 1 computable SELL, 5 statement FX rates incl. 31.57), ledger back to 59, cost_basis_state re-persisted (BBAI/GLD/GOOG/JEPQ/QQQI remaining positions, drained symbols dropped), single document row (no duplicates). npm test 330 PASS/0 FAIL; test:w2 140 PASS/0 FAIL; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Bank-of-Thailand (BOT) dependency REMOVED; FX resolution is now Statement -> Keyless Historical FX Provider -> THB=1. Deleted app/lib/bot-exchange-rate.ts; new app/lib/historical-fx-provider.ts (keyless Frankfurter/ECB provider behind a `HistoricalFxSource` abstraction, `resolveHistoricalFxRate` cache-first via exchange_rate_cache, source label "historical-fx-provider", graceful null on any failure — never throws, never fabricates, no BOT_API_KEY anywhere). `GET /api/v1/exchange-rates` re-pointed to the provider (not-available reason no longer mentions BOT; no-currency mode serves cached rates + THB=1). `app/routes/api/exchange-rates/status.ts` + `app/routes/api/admin/stats.ts` now report `fxProvider` (built_in/always configured) instead of `bot`; Admindashboard row/labels updated. Pipeline gains `applyFxRateFallback` (app/lib/statement-pipeline.ts, pure + injectable, Decimal): applied ONLY to non-THB rows without a statement rate, lands in fx_rate_effective, recomputes amountThb + realizedGainLossThb, never touches fx_rate_statement/fx_rate_bot, provider unavailable/throws -> base fallback kept (import unaffected). `app/routes/api/statements/upload.ts` applies it in BOTH fresh + rebuild paths before insert. UI: Dashboard FX card external label "จาก Historical FX Provider"/"แหล่งที่มา: Historical FX Provider" (was "อ้างอิงจากภายนอก"/"External historical rate"), FxAiPage FX status card renamed (key "fx", zero BOT references), `botRate` state renamed `fxRateEntry`/`fxEntry`, server-api/Financeutils/capital-ledgers/schema comments de-BOT-ified. fx_rate_bot column KEPT as legacy compatibility (manual ledger user-entered rates unchanged; not an FX source). Gemini prompts strengthened (statement-prompt.ts + gemini-statement-parser.ts): exchangeRate/amountThb ONLY if explicitly printed in the document, never guessed/inferred; untouched determinism confirmed (Gemini result is preview-only, never inserted). Tests: run-tests.mts BOT block rewritten as "Historical FX provider (MOCKED)" — asserts BOT_API_KEY no longer required, provider endpoint + source label, 404 -> available:false no invented rate, THB=1 without network, cached rate served with provider down, no BOT endpoint ever contacted; test-statement-tax-recon.mts +9 FX-priority tests (statement 31.57 wins, fallback only when statement absent, THB never overridden, unavailable graceful, no invented P&L, fx_rate_bot untouched); test-ui-placeholders.mts adds Historical FX Provider wording / no-BOT / no-bot-exchange-rate / no-BOT_API_KEY checks. Verified live on dev data: fx rate for 2026-01-30 USD = 31.425 (source historical-fx-provider), THB=1, status returns fxProvider built_in/configured, admin stats 3/3, ledger still 59 rows with NVDA SELL keeping statement FX 31.57. npm test 344 PASS/0 FAIL (was 330); test:w2 146 PASS/0 FAIL (was 140); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Final backend audit of feat/complete-backend-systems completed with 3 blocker fixes + 2 cleanups, all rerun-verified:
  - Fix 1 (upload.ts): fresh-import path no longer persists cost_basis_state BEFORE the rows are inserted — `saveCostBasisState` moved AFTER `insertStatementTransactions` (mirrors the rebuild path) and is now best-effort (try/catch, log-only) in BOTH fresh + rebuild paths, so a DB failure mid-import can no longer poison the running-average basis with phantom rows, and a basis-write failure can no longer 500 an already-committed import or push it into the cleanup path that deletes the document row while ledger rows are committed (orphan rows + duplicate-on-reupload risk).
  - Fix 2 (audit-log.ts): `insertAuditLog` is now best-effort/non-throwing (try/catch + console.error) — an audit-write failure can no longer turn a successful login/import/delete into a 500 or trigger error/cleanup paths. Stops the post-commit cleanup window in upload.ts (audit + notification were the only throw-capable steps after the atomic insert).
  - Fix 3 (analysis.ts): repaired the two mojibake Thai error strings (lines 101 + 150, UTF-8 controls) — now proper `ยังไม่มีธุรกรรมที่ต้องการวิเคราะห์` and `การวิเคราะห์ด้วย Gemini ไม่พร้อมใช้งาน`. Also confirmed `data.code` only ever carries stable machine codes (gemini_*), never upstream error text.
  - Fix 4 (CapitalLedgerPage.tsx): editing an imported (AI_PARSED) row no longer crashes — `openEditModal` prefills the rate field from `fx_rate_bot ?? fx_rate_effective ?? fx_rate_statement ?? "1"` (fxRateBot is null for imported rows) and the ApiTransaction type allows `fxRateBot: string | null`, so a simple amount edit can no longer throw on `form.rate.trim()` nor silently reset an imported row's FX to 1.
  - Cleanup: removed 3 debug `console.log("[ACCOUNT_STATUS] ...")` traces from ProtectedLayout.tsx (fired on every protected mount).
  - Audit verified (no defects found): git inventory clean (no .env/dev.db/storage/build/re-act-router tracked); no secrets/VITE_* anywhere; all 24 API routes registered in app/routes.ts and user-scoped; migrations 0007–0010 non-destructive and schema-consistent; gemini parser/analysis never leak keys or statement text and degrade gracefully (429 -> available:false gEMINI_REQUEST_FAILED, confirmed live); parser/pipeline financial math sound (fees signed, proceeds=broker net, realized only on computable SELL, Decimal THB conversion, single conversion no double FX); frontend/backend contract field-for-field (no BOT wording, no P&L/tax computation in React). Left as WARNINGS (not fixed, not blockers): Invoice of a few unused items — notifyAccountStatusChanged (no callers), dailyTaxSummaries table (no consumers), extractTextFromPdf filePath overload (dead), GET /api/v1/documents/:id (no DELETE-paired read), /aggregation/daily + /exchange-rates/status registered but only admin uses status; validateAmount accepts Infinity/whitespace; exchange-rates accepts non-calendar dates/arbitrary currencies (graceful provider null); cost-basis replay uses parseFloat (mirrors parser).
  - Final battery: npm test 344 PASS/0 FAIL; test:w2 146 PASS/0 FAIL; typecheck + build clean; git diff --check clean (CRLF notice only). Live dev smoke (localhost:5173 + dev Supabase): USER/ADMIN login + roles, session, ledger 59 rows, NVDA SELL 2026-01-15 fx 31.57/31.57 with realizedGainLossThb null (honest non-computable), documents=1 + own-download 200 pdf, tax/calculate 59 rows computable=1 totalTaxableAmountThb=107.97 (no double conversion), FX USD 2026-01-30=31.425 source historical-fx-provider, THB=1, USER blocked from admin stats (403), ADMIN stats 3/3 + fxProvider built_in. Changes NOT committed, NOT deployed.

- Double-entry general ledger backend built + fully wired. Pure engine app/lib/general-ledger.ts (DB-free, Decimal): exactly-one-leg lines, per-currency balance, THB from fxRateEffective, ISO date + strict 3-letter ISO-4217 currency checks, trialBalance/incomeStatement/balanceSheet (opening + net-income folding: A = L + E + NI), buildReversal. Service app/lib/ledger-service.ts (Drizzle/Postgres): seedDefaultChartOfAccounts (14 THB/USD accounts), createJournalEntry now resolves line.accountId by code OR id to the user's account UUID (posting-engine is code-based, schema FK is id-based — fixed during wiring) and enforces account-currency match at write (mismatch -> 422; statement postings skip such rows gracefully), lazy-seeds CoA for pre-existing users, insertPostings best-effort, listJournalEntries/getAccountLedger/getTrialBalance/getIncomeStatement/getBalanceSheet, reverseJournalEntry. Posting engine app/lib/posting-engine.ts (pure) maps ValidatedCapitalRow (now persists category + section via migrations 0012/0013) to equity/asset BUY (fees capitalized)/asset SELL (gain/loss split, honest no-basis)/income/expense entries; currency-exchange-only rows not auto-posted; migration 0011 adds accounts/journal_entries/journal_entry_lines with a line-level single-leg CHECK. WIRING: register seeds the CoA (best-effort); statements/upload posts imported rows in fresh + rebuild paths after commit (additive posting:{posted,entryNumbers,skippedRows,skippedWrite} in response + audit); new user-scoped routes in app/routes.ts — GET/POST /api/v1/accounts, GET/POST /api/v1/journal, POST /api/v1/journal/:id/reverse, GET /api/v1/ledger/accounts/:accountId, GET /api/v1/reports/trial-balance, GET /api/v1/reports/income-statement, GET /api/v1/reports/balance-sheet (?from=&to= ISO filters, verifyAuth-protected); audit actions ACCOUNT_CREATE/JOURNAL_ENTRY_CREATE/JOURNAL_ENTRY_REVERSE added. Tests: scripts/test-general-ledger.mts (35 asserts) wired into npm test; run-tests.mts gains "REG: GENERAL LEDGER WIRING" (real routes: register->CoA, create-by-code 201, currency-mismatch 422, journal list, trial-balance balanced, reversal 200) and FK-safe CLEANUP (removes journal_entry_lines -> journal_entries -> accounts for smoke users + USER A before user delete). Local verification: npm test 478 PASS/0 FAIL (pure suites instantiate a postgres client at import — set a placeholder DATABASE_URL, zero queries run); typecheck + build clean; git diff --check clean (CRLF notice only). test:w2 additions NOT run locally (no TEST_DATABASE_URL here). Changes NOT committed.

- General Ledger UI built (สเปกที่อนุมัติ เริ่มได้) without touching existing frontend features: new sidebar item "บัญชีแยกประเภท" (icon ScrollText, after "สมุดบัญชี") in Dashboard.tsx renders `app/component/Ledger/GeneralLedgerPage.tsx` with 5 sub-tabs — ผังบัญชี (list + add-account modal, row-click → per-account ledger with running balance), บันทึกรายการ (journal list, period filter, manual-entry modal with per-line debit/credit/fx/memo + per-currency live totals, reverse-entry with confirm; auto-posted entries from STATEMENT are shown in the list and reversal is allowed on any POSTED entry), งบทดลอง, งบกำไรขาดทุน, งบดุล (as-of). All values rendered server-authoritative; No P&L/FX recompute in React. New client helpers + DTO types in app/lib/server-api.ts (fetchAccounts, fetchJournal, createJournalEntry, reverseJournalEntry, fetchAccountLedger, fetchTrialBalance/IncomeStatement/BalanceSheet). Reused existing design tokens (banner from-blue-900, white rounded-xl cards, blue-900 buttons, emerald/red badges, skeleton/error/empty states). PdfStatementUploader, StatementArchivePage, StoredDocumentsList, CapitalLedgerPage NOT modified; GL links to archive tab via onNavigateToArchive. Verification: typecheck EXIT 0, build EXIT 0, npm test 451 PASS/0 FAIL (measured total across 14 printing suites; every suite FAIL: 0, exit 0), git diff --check clean (CRLF notice only); test:w2 still requires TEST_DATABASE_URL. Changes NOT committed.

- Final Feature List gaps closed (3 approved items, no UI redesign — matches the reverted-restructure constraints):
  1. Nav reachability: Dashboard.tsx nav gains "ปฏิทินยอดเงิน" (CalendarDays) + "ฐานภาษี" (Calculator) after "คลัง Statement"; Dashboard now fetches capital ledger + documents ONCE at the shell (shared `fetchCapitalLedger` → `capitalLedgerToTransactions` + `fetchUserDocuments`, error banner on failure) and renders `<DailyCalendarExport transactions documents/>` + `<FxAiPage transactions onImport onDocumentSaved/>`; FxAiPage `onImport`/`onDocumentSaved` refetch the shared source (no local session state).
  2. Day → Statements: `Transaction.sourceDocumentId` added (Financeutils) and mapped in server-api `capitalRowToTransaction`; `DailyCalendarExport` accepts an optional `documents?: ServerDocumentMeta[]` prop and the selected-day detail panel shows "Statement ของวันนี้" — doc ids derived from the day's transactions' sourceDocumentId, files downloaded via the existing `downloadUserDocument` (server Blob) — no client-side guessing.
  3. Tax Summary export: `app/lib/export-service.ts` gains pure `buildTaxSummaryRows` (reuses `buildTaxRecon` from tax-engine — same classification/totals as POST /api/v1/tax/calculate, only computable rows contribute, non-computable rows explicit) and `exportTaxSummary` (per-transaction Thai columns + summary block; CSV w/ BOM + XLSX with "Tax Summary" + "สรุป" sheets, filename `stax_tax_summary_<date>`). `POST /api/v1/export` gains `report: "tax"` (default = capital). Client helper `exportTaxSummary(accessToken, format, opts)` in server-api. FxAiPage tax card header adds "ส่งออก CSV" / "ส่งออก Excel" buttons (only when the tax result is loaded).
  Tests: test-server-api.mts +5 (sourceDocumentId map-through + 5 buildTaxSummaryRows assertions; now imports export-service via _load-env); test-ui-placeholders.mts +11 (nav labels, shared server fetch, documents prop, "Statement ของวันนี้", export buttons/route). npm test EXIT 0 all 16 suites FAIL: 0 (server-api 44, ui-placeholders 64, general-ledger sub-sections, etc.); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.
- FxAiPage de-jam done (minimal UI fixes only, approved: "บีหน้า ฐานภาษี แน่นเกินไป" → ปรับเฉพาะจุดที่งง): Tax card moved from 5th position to FIRST after the banner (order now Banner → ฐานภาษี → อัตราแลกเปลี่ยน → สถานะ → AI table → สรุปโดย AI); banner renamed to ฐานภาษี / "ฐานภาษีคำนวณจากกำไรที่รับรู้แล้วจากการขายหุ้น" (was "Foreign Exchange & AI / อัตราแลกเปลี่ยน AI"); jargon removed from visible labels — tax card h2 "การคำนวณฐานภาษี (Tax Core Engine)" → "ฐานภาษีจากกำไรที่รับรู้แล้ว", status fx card "อัตราแลกเปลี่ยน (Historical FX Provider)" → "แหล่งอัตราอ้างอิง" + detail "อัตราอ้างอิงจากแหล่งข้อมูลภายนอก", tax status "Tax Core Engine พร้อมใช้งาน" → "พร้อมใช้งาน", AI insight "สรุปกิจกรรมจาก CI Insight" → "สรุปโดย AI". Dense-table fix: new client `showAllTaxRows` state (default false) — default renders only `realized-gain` + `non-computable` rows (the rows that touch the tax base), hides `buy-basis`/`not-applicable` with "ซ่อน N แถว (BUY/กระแสเงินสด)" in the summary box + a header toggle "แสดงทุกแถว" / "แสดงเฉพาะที่ต้องดู" (Eye/EyeOff); summary totals stay server-authoritative (buildTaxRecon), empty-when-filtered state explains where the hidden rows went instead of a false "no transactions". Colors/layout tokens unchanged, other pages untouched. tests: test-ui-placeholders.mts section 7 +7 (banner renamed + no "อัตราแลกเปลี่ยน AI", jargon-free labels, tax-before-FX order via indexOf, "แสดงทุกแถว" toggle, realized-gain/non-computable filter) → 71 PASS; npm test EXIT 0 (all suites FAIL: 0); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.
- Gain/loss backfill B + A done (approved; GL re-posting skipped "ข้ามไปก่อน"): migration `drizzle/0016_add_capital_transactions_net_amount.sql` (`ALTER TABLE "Capital_Transactions" ADD COLUMN net_amount numeric`, applies to dev DB); `app/db/schema.ts` gains `netAmount: numeric("net_amount")`; statement-pipeline inserts `row.netAmount` and gains a pure `computeGainLossBackfill(rows, actions)` engine + `backfillComputedGainLoss(userId)` wrapper. The engine replays the FULL ledger chronologically with the parser's average-cost math and fills ONLY frozen holes: AI_PARSED SELL rows with empty realizedGainLossThb that now have sufficient basis; `net = net_amount ?? (gross − fees)` (never overwrites stored proceeds), `fx = stored fx_rate_effective` or THB→1 (never invented), skips MANUAL + already-computed rows, reduces qty for ALL SELLs (parser parity), handles same-date determinism (both pure sorts now tie-break: trades before same-date corporate actions, then transactionId/action.id — and both wrappers SELECT with `ORDER BY transaction_date, transaction_id` so wrapper, pure replay, and `recomputeCostBasisMap` cannot disagree; recomputeCostBasisMap additionally gains optional `transactionId` on CostBasisReplayRow). THB conversion now mirrors the parser exactly (gainThb derived from the ROUNDED gain; verified invariant realizedGainLossThb === round2(round2(gain) × fx) holds for every computed row). corporate_actions missing on a deployment (0014 not migrated, e.g. dev DB) is tolerated — lookup falls back to `[]` with no block. Triggers wired best-effort in upload.ts (fresh + rebuild paths after saveCostBasisState), documents.$id.ts, capital-ledgers.$id.ts; one-shot CLI `scripts/backfill-gain-loss.mts` (runs every user or a filter argument). Frontend Option A: FxAiPage `taxVisibleRows` returns `counts {sellNonComputable, notTaxable}` and the summary box now splits an amber `SELL ที่ยังคำนวณไม่ได้ X รายการ · คำนวณได้ Y รายการ` from a muted `BUY/กระแสเงินสด Z รายการไม่ใช้คำนวณ` (no lumped "ยังคำนวณไม่ได้" total) — server fields/export untouched. Applied to dev DB (docker localhost:55432): migration 0016 applied; backfill filled 26 rows across the two real users; verified dev state converges — user 9f5a2c2b 315 rows → 20 computable SELLs / 8 non-computable (was 9/19), user 51a124a0 374 rows → 30 computable / 9, both with replay drift 0 and invariantBad 0. Tests: test-statement-tax-recon 11 backfill asserts (frozen fill, net=gross−fees, stored net wins, never overwrite/skip manual/no-basis/no-fx, THB=1, per-symbol interleave), test-ui-placeholders +2 ("SELL ที่ยังคำนวณไม่ได้", `sellNonComputable`/`notTaxable`), run-tests.mts REG: GAIN/LOSS BACKFILL block before CLEANUP (gated on net_amount column presence; expected 100/290/190/6729.80). npm test EXIT 0 (all suites FAIL: 0, ui-placeholders now 73); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Standalone "อัตราแลกเปลี่ยน ณ วันที่ทำรายการ" FX-list card REMOVED from the ฐานภาษี page (FxAiPage.tsx) — deleted the whole card (title/badge/empty-state/table), dropped the now-dead `fxRows` selector + unused `LineChart` import; kept the per-row "อัตรา FX" column in the tax table and the "แหล่งอัตราอ้างอิง" source status card (both confirmed by user). Page order now Banner → ฐานภาษี (tax table) → สถานะ cards → ผลการวิเคราะห์ Statement ด้วย Gemini → สรุปโดย AI; no FX data removed at the data/backend level. test-ui-placeholders.mts: replaced the base-first ordering check (anchor "อัตราแลกเปลี่ยน ณ วันที่ทำรายการ") with two asserts — the removed string is GONE ("Standalone statement-FX list card removed") + tax card still renders before the later AI-result section ("ผลการวิเคราะห์ Statement ด้วย Gemini" as JSX-side anchor, since statusCards titles are defined earlier in the file). ui-placeholders now 74 PASS/0 FAIL; npm test EXIT 0 (all suites FAIL: 0); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Dashboard หน้าหลัก page built (สเปกที่อนุมัติ: เมนูใหม่ + เป็นหน้าเริ่ม + 4 วิดเจ็ต + ใช้ API เดิมเท่านั้น): new `app/component/DashboardUser/DashboardHomePage.tsx` — banner สวัสดีสีน้ำเงินจาก-blue-900 พร้อมปุ่มด่วน (อัปโหลด/คลัง/ปฏิทิน/ฐานภาษี) + 4 วิดเจ็ต server-authoritative: สรุปงบการเงิน (per-currency cards จาก `fetchLedgerSummary` totalsByCurrency + สมดุล badge) / การถือครองหุ้น (จาก `fetchCostBasis`, ต้นทุนเฉลี่ย) / สถานะฐานภาษี (จาก `fetchTaxRecon(accessToken, transactions.map(t=>t.id))` — แสดง totalTaxableAmountThb + computedCount/nonComputableCount, ข้อความสุจริต "ยังไม่สามารถคำนวณฐานภาษีรวมได้ เนื่องจากข้อมูลต้นทุนยังไม่ครบ" เมื่อ !computable, ข้าม call เมื่อไม่มีธุรกรรม เพราะ route ต้องการ ≥1 id) / เอกสาร Statement (นับ + 5 ล่าสุดจาก prop documents) + ธุรกรรมล่าสุด (8 รายการจาก prop transactions) — ปุ่มลิงก์ทุกการ์ดไปหน้าต่างๆ ผ่าน `onNavigate`. ไม่มี P&L/tax คำนวณใน React; `fetchTaxRecon` ใช้ `Promise.allSettled` แยกวิดเจ็ต (error เฉพาะเมื่อ summary+holdings ล้มทั้งคู่). `Dashboard.tsx`: `NavId` เติม "dashboard", navItems แทรก "หน้าหลัก" (LayoutDashboard, ไอคอนที่เคย import แต่ไม่ได้ใช้) เป็นรายการแรก, default `useState<NavId>("dashboard")` (เดิม "gl") ทำให้ล็อกอินเข้า หน้าหลัก ทันที, render branch ใหม่รองรับ ledgerError banner แบบเดียวกับ calendar/tax, ส่ง test shared serverTransactions/serverDocuments. หน้าอื่น (FxAiPage/GL/calendar/archive/upload) ไม่แตะ. tests: test-ui-placeholders.mts section 8 +8 asserts (nav+default, page นำเข้า DashboardHomePage, ใช้ 3 fetcher, `Promise.allSettled`+`transactions.map(tx=>tx.id)`, วิดเจ็ตทั้ง 4, ข้อความไม่คำนวณปลอม, ปุ่มด่วน) → ui-placeholders 82 PASS/0 FAIL; npm test EXIT 0 (all suites FAIL:0); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Webull CSV import feature REMOVED (approved): deleted `app/routes/api/import/csv.ts` + `app/lib/csv-import/` (6 files: csv-parse, csv-types, row-hash, build-rows, symbol-name-map, csv-import-store) + `app/lib/storage/csv-storage.ts` + `scripts/test-csv-import.mts`; removed `api/v1/import/csv` route from app/routes.ts, `csvImportRows` table from app/db/schema.ts, `CsvImportType`/`CsvImportStats`/`CsvImportResult`/`importCsv` from app/lib/server-api.ts, `notifyCsvUploaded/Imported/Duplicate` from notification-service.ts; `.csv` no longer accepted in storage-driver.ts (regex `\.(pdf|csv)$` → `\.pdf$`, `buildObjectKey` extension narrowed to `"pdf"`); StatementUploadPage.tsx cleaned of the entire "นำเข้า Webull CSV" card (PDF dropzone untouched); `csv-parse` dependency + `test-csv-import.mts` dropped from package.json (lockfile synced). KEPT: Webull PDF parser, CSV/XLSX export, migration 0015 + existing csv_import_rows data as legacy (no drop migration). npm test all 15 suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Daily stock-price table built (approved: Yahoo Finance keyless + Dashboard หน้าหลัก + Cron + lazy refresh + ประวัติรายวัน). Migration `drizzle/0017_add_stock_prices.sql` + `app/db/schema.ts` gains global (NOT user-scoped) `stock_prices`: symbol, price_date, close_price, currency, source, created_at/updated_at, uniqueIndex(symbol, price_date) — market reference data shared by all users, never a tax/ledger input. `app/lib/stock-price-provider.ts` (pure, mirrors historical-fx-provider): `parseYahooChartResponse` (last real close + UTC date + currency), `yahooFinanceStockSource` (keyless chart API, 15s timeout, graceful null — never fabricates), `isDefaultStale` 1-day gate, DB-free `refreshStockPricesCore(symbols, source, upsert)` with polymorphic delay + failed[] stats, `stockPriceRowFromQuote`. `app/lib/stock-price-cache.ts`: getLatestStockPrice / getLatestStockPricesForSymbols / idempotent upsertStockPrice. `app/lib/stock-price-refresh.ts` (DB): `getTrackedSymbols` (DISTINCT symbol from Capital_Transactions), `refreshStockPrices` (daily sweep), `resolveStockPrice`/`resolveStockQuotes` (cache-first → lazy provider → stale row fallback). Routes registered in app/routes.ts: `GET /api/v1/stock-prices?symbols=` (user-scoped read, lazy refresh) + `POST /api/v1/stock-prices/refresh` (Vercel cron via `Authorization: Bearer <CRON_SECRET>` or `x-cron-secret` header when CRON_SECRET set, else ADMIN JWT; bare GET loader → 405). New `vercel.json` crons `30 17 * * *` (00:30 ICT). Client `server-api.ts`: `StockPriceQuote` + `fetchStockQuotes` + display-only `holdingMarketValue`/`holdingUnrealizedPnl`/`quoteForSymbol`. DashboardHomePage "การถือครองหุ้น" card adds ราคาปัจจุบัน (quote + ราคาวันที่), มูลค่าตลาด (qty×close), กำไร/ขาดทุน unrealized ((close−avgCost)×qty, emerald/red) — honest "-" when no price, caption clarifies display-only ไม่เกี่ยวกับการคำนวณฐานภาษี; load() fetches quotes only when holdings exist via Promise.allSettled. Tests: new `scripts/test-stock-prices.mts` (22 asserts: parsing/error/malformed, UTC date format, staleness boundary, mocked-fetch source success/non-2xx/bad-JSON, refresh-core stats + failed symbols + upsert payloads, row shaping) wired into `npm test`; test-ui-placeholders.mts section 9 +11 (routes registered, schema/index/global-data comment, provider exports, client helpers, UI wiring + columns + display-only label, vercel cron); run-tests.mts REG: STOCK PRICES block (gated on stock_prices table, mocked Yahoo fetch, real routes: anon 401, USER 403, bare GET 405, ADMIN sweep stats, cached close 123.45 served without re-fetch, x-cron-secret allow, cleanup stock_prices rows written). npm test EXIT 0 all 16 suites FAIL: 0; ui-placeholders 99 PASS; typecheck + build clean; git diff --check clean (CRLF notice only). test:w2 REG block NOT run locally (no TEST_DATABASE_URL). Changes NOT committed.

- Daily-calendar/export feature REMOVED (approved — user: "ไม่สามารถดูอะไรได้เลยจากฟีเจอร์นี้"; Export buttons removed along with it, not moved): deleted `app/component/DashboardUser/Dailycalendarexport.tsx` (566 lines). `Dashboard.tsx`: removed `CalendarDays` icon import + `import DailyCalendarExport`, dropped `"calendar"` from `NavId`, removed the `{ id: "calendar", label: "ปฏิทินยอดเงิน" }` navItems entry and the whole `activeNav === "calendar"` render block (incl. its ledgerError banner) — `serverTransactions`/`serverDocuments`/`ledgerError` KEPT because DashboardHomePage still renders from them. `DashboardHomePage.tsx`: removed the `CalendarDays` import, `"calendar"` from `DashboardNav`, the `{ nav: "calendar", label: "ปฏิทินยอดเงิน" }` QUICK_ACTIONS entry, and the "ดูปฏิทิน" quick button in the cash-card header (kept "ดูทั้งหมด" → cashflow). Sidebar now has 5 items (หน้าหลัก/บัญชีแยกประเภท/อัปโหลด Statement/คลัง Statement/เงินเข้า-ออก). NOT touching shared backend: `/api/v1/capital-ledgers`, `/api/v1/documents`, `/api/v1/documents/:id/download`, `/api/v1/export` + `server-api.ts` fetchers + `Financeutils` still used by other pages. Tests: test-ui-placeholders.mts removed the whole Section 7 (6 calendar asserts + the `read()` of the deleted file) and converted two Section 8 asserts — "Cash summary card links to the calendar" → asserts the calendar link is GONE (`!ดูปฏิทิน && !onNavigate("calendar")`), quick-links assert no longer requires `{ nav: "calendar" }`; test-server-api.mts "feeds the calendar/tax views" wording → "home view". ui-placeholders drops from 79 to 73 passes (all removed asserts were calendar ones); npm test all 16 suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Cash in/out ("เงินเข้า-ออก") all-time view now lists transactions chronologically by date: `CashFlowPage.tsx` and `DashboardHomePage.tsx` request `{ withDetail: true }` for EVERY cash-summary view (previously only month/asOf), and the `all` (ภาพรวม) view renders the monthly breakdown table on top + a per-transaction rows table below (รายการทั้งหมด N รายการ · เรียงตามวันที่), ordered oldest-first as the server already returns (`buildCashSummary` sorts detail rows ascending by transactionDate). Backend untouched — `withDetail` was already scope-independent on `GET /api/v1/cash-summary`. Footnotes updated ("ตารางล่างเรียงตามวันที่ (เก่าก่อน)"). Tests: test-ui-placeholders.mts +5 (constant withDetail on the shared query, all-view rows table + เรียงตามวันที่, honest empty state, oldest-first footnote) → 77 PASS; test-cash-summary.mts +2 (all-time withDetail lists every counted row oldest-first, monthly breakdown + totals intact) → 16 PASS. npm test all 15 suites FAIL: 0; typecheck + build clean. Changes NOT committed.

- Cash in/out "เงินเข้า/ออก" three improvements REVERTED (user: "ย้อนกลับไปก่อนได้ไหม" → "Going back to before the cut-off date feature was removed"): restored the ตัดยอด ณ วันที่ (asOf) UI and removed the description/ที่มา column + sort toggle. EXACTLY what was undone: `CashFlowPage.tsx` + `DashboardHomePage.tsx` — `CashView` back to `"all" | "month" | "asOf"` (was `"all" | "month"`), `asOf`/`cashAsOf` state + date picker restored, 3-view switcher restored (ภาพรวม/รายเดือน/ตัดยอด ณ วันที่), sort toggle (`sortDir`/`sortedRows`/`ArrowUpDown`) REMOVED, both pages render `cashRows`/`rows` unsorted (server chronological order), "ที่มา" column (`r.description`) REMOVED (colSpan back to 5), footnotes back to "ตารางล่างเรียงตามวันที่ (เก่าก่อน)". `CapitalLedgerPage.tsx`: `FormState`/`emptyForm`/`openEditModal`/`buildPayload` back to pre-description shape, modal "ที่มา / รายละเอียด" input removed, ledger table "ที่มา" column removed, `ApiTransaction.description` removed. Backend: `app/db/schema.ts` `Capital_Transactions.description: text("description")` removed; `drizzle/0018_add_capital_transactions_description.sql` DELETED (never applied to any DB); `statement-pipeline.ts` no longer maps/persists `description` (ValidatedCapitalRow+mapToCapitalRow+insertStatementTransactions); `capital-ledgers.ts` POST no longer accepts/normalizes/persists `description`; `capital-ledgers.$id.ts` PUT no longer accepts `description`; `cash-summary.ts` `CashSummaryDetailRow.description` + buildCashSummary passthrough + getCashSummary SELECT removed; `server-api.ts` client `CashSummaryDetailRow.description` + `CapitalLedgerRow.description` removed. Tests reverted: test-cash-summary.mts — fixture `description` field + test 12b (description passthrough) removed → 16 PASS; test-ui-placeholders.mts — new reads (cashFlowPage/capitalLedgerPage/cashSummaryLib/statementPipeline/descriptionMigration) removed, cash asserts restored to 3-view + asOf + month AND date pickers + no-asOf-removed, sort-toggle + "ที่มา" + migration-0018 asserts removed → 77 PASS. Root cause of the runtime "Failed to load capital ledger from the server" (schema column `description` referenced by Drizzle SELECT but column never migrated into the actual DB) is now GONE because the schema is back in sync with the DB (26 columns, no extra). npm test EXIT 0 (all suites FAIL: 0); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- "ตัดยอด ณ วันที่" (asOf) viewing improved (approved A+B+C, UI-only on both cash pages, no backend): 1) As-of totals now say เงินเข้า/เงินออก/ยอดสุทธิ **สะสม** with the cutoff date in Thai (`เงินเข้าสะสม (${fmtDate(asOf)})` etc.) instead of the misleading "เฉพาะช่วง" label that made the accumulated numbers hard to understand; 2) asOf rows table gains a **ยอดสะสม (THB)** running-net column (client-side `runningBalance`/`cashRunningBalance` useMemo over the pre-sorted rows, display-only, blue + or red − coloring) so the account net at each cutoff date is readable at a glance; 3) all asOf dates formatted Thai — rows-table header `รายการตัดยอดถึงวันที่ {fmtDate(...)}` and row dates in CashFlowPage (`fmtDate` helper added; DashboardHomePage already used `fmtDate`); footnotes updated to explain the running column (CashFlowPage) / that totals are accumulated to the cutoff (DashboardHomePage). No `cash-summary.ts` change; no P&L/tax logic added. Tests: test-ui-placeholders.mts +6 (cashFlowPage now read; as-of เงินเข้าสะสม/เงินออกสะสม/ยอดสุทธิสะสม + fmtDate(cashAsOf), running-balance column helper present in both pages, asOf table header Thai on both) → 82 PASS/0 FAIL. npm test EXIT 0 all suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- General-Ledger revenue categories fixed ("มา thinkcache ว่าข้อมูลปนกัน" — confirmed cause and fixed): the parser had always emitted — for EVERY computable SELL — BOTH the asset SELL row (gain posted to 4010 กำไรจากการขายหลักทรัพย์) AND a redundant income row with `section = "กำไรจากการขายหุ้น"` that `incomeAccountFor()` defaulted to DIVIDEND_INCOME (4020). Net effect: every realized gain was POSTED TWICE (once to 4010, once to 4020) and the เงินปันผล account was polluted with capital gains — the "mixed up" the user saw. Fixes in `app/lib/posting-engine.ts`: (1) `isCapitalGainIncomeRow()` — income rows whose section is exactly `กำไรจากการขายหุ้น` are NO LONGER posted (returned as skipped with reason "realized gain/loss already posted via the SELL row"); the SELL asset row's own 4010/5120 split remains the single source of truth, so no double-count and Dividends is clean; (2) `incomeAccountFor()` defensive: any income section containing `กำไรจากการขาย` routes to 4010, never Dividends; (3) the dividend income CREDIT leg now carries `memo` = the uppercase stock symbol so the 4020 account ledger shows WHICH stock each dividend came from (`dividendSymbolFor` reads row.symbol, else parses the `เงินปันผล:X` section). Supporting change in `app/lib/statement-pipeline.ts` `mapToCapitalRow`: dividend income rows now derive their `symbol` column from the section label (`เงินปันผล:goog` → `GOOG`; explicit t.symbol preserved verbatim; unknown-marker sections stay null) so the per-stock key is available everywhere. One-shot repair script `scripts/backfill-revenue-categories.mts` (DB-backed, safe to re-run, optional `[userId]` arg): (1) REVERSES existing journal entries created from the duplicate `กำไรจากการขายหุ้น` income rows onto account 4020 (reversal entries, the SELL row's 4010 posting untouched — verified the entry really credits the user's 4020 before reversing); (1b) also catches ORPHANED duplicate gain entries whose source Capital_Transactions row was deleted (identified by journal `description = "กำไรจากการขายหุ้น"` + source_type STATEMENT + a 4020 line) — this closed the last POSTED 4020 STATEMENT entries; (2) back-fills `symbol` on dividend Capital_Transactions rows whose section embeds the ticker; (3) back-fills `memo` = symbol on the existing 4020 credit line of every dividend posting. APPLIED to dev DB: 33 4020 STATEMENT entries reversed (28 matched source rows across 9f5a2c2b/0ca8cd7e/51a124a0/d8a4e289 + 5 orphaned for fc90cb0b), re-run is a no-op (reversed=0), 4010 gain postings (24) untouched, zero POSTED 4020 statements remain; dev data has NO `เงินปันผล:` dividend rows (only `กำไรจากการขายหุ้น` 28 + `ดอกเบี้ย` 22 income sections) so symbol/memo backfill is a no-op there by design. Tests: test-general-ledger.mts +10 pure posting-engine asserts (dividend→4020 + memo NVDA, interest→4030 no memo, duplicate capital-gain row skipped, buildStatementPostings includes only the real dividend row, `กำไรจากการขายหลักทรัพย์` → 4010) → 45 PASS; test-statement-tax-recon.mts +4 dividend-symbol derivation asserts → 49 PASS; test-webull-trade-parser.mts unchanged (parser still emits the informational income row, it's just not posted anymore). npm test EXIT 0 all suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Dividend-account per-stock reclassification done (approved at the time), THEN reverted to account-name-consistent roles (user: "4010 data missing / classification inverted"), THEN — per the user's follow-up instruction ("Let's go back to before we fixed these two things and make a new plan") — the ORIGINAL pre-correction 4020 content was RESTORED where reconstructable. Timeline: (a) the earlier `scripts/reclassify-ledger-dividends.mts` (DELETED) had swapped DATA against account names (24 STATEMENT gain lines 4010→4020 + flat MANUAL `รับเงินปันผล` 4020→4010); (b) `scripts/revert-reclassify-ledger-dividends.mts` undid that (24 → 4010 with kept per-stock memo, `รับเงินปันผล` 150 → 4020; re-run no-op; VERIFY tolerance 0.01); (c) NEW `scripts/restore-ledger-4020-original.mts` (idempotent, optional `[userId]` + `--dry-run`) recreates the 28 deleted STATEMENT "กำไรจากการขายหุ้น" entries that cleanup-ledger-4020.mts had deleted — source rows survived in Capital_Transactions (category income, section กำไรจากการขายหุ้น) so each entry is rebuilt exactly: Dr cash(1020) / Cr 4020, amount=amount_foreign, fx=fx_rate_effective, fxRateStatement=fx_rate_statement, entryDate=transaction_date, description "กำไรจากการขายหุ้น", sourceType STATEMENT, sourceTransactionId + sourceDocumentId linked, memo NULL; skips if already exists. Applied to dev DB: restored 28 (9f5a2c2b=9/102.66, 51a124a0=10/115.18, 0ca8cd7e=8/98.54, d8a4e289=1/3.42); fc90cb0b's 5 orphans NOT recoverable (its Capital_Transactions rows deleted, cap_tx=0). 4020 now again shows the historical double-posted gains (per the user's chosen "go back before corrections" baseline); 4010 untouched (24 gain lines). Posting engine unchanged — NEW imports still skip the duplicate gain row (isCapitalGainIncomeRow), so this restored data is purely historical baseline for a future new plan, not a re-enabled behavior. Ledger balanced (new entries Cr=Dr=319.80 exact; whole-ledger −0.02 = pre-existing one-cent rounding entries #241/#258). Tests unchanged: npm test all suites FAIL 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- GL accounts 4010/4020 content SWAPPED (approved: "swap content 4010 ↔ 4020, ครบทุกบรรทัด" + "swap layout in engine ด้วย"): new `scripts/swap-ledger-4010-4020.mts` (data-level, optional `[userId]` + `--dry-run` + `--force`; orientation-guarded — refuses to re-run when per-stock gain STATEMENT lines are already on 4020 unless `--force`; single-transaction CASE UPDATE per user; post-swap count verification throws on mismatch; whole lines moved between two INCOME accounts ⇒ per-entry per-currency balance preserved, trial balance unchanged). Applied to dev DB: per-stock STATEMENT gain lines (กำไรจากการขาย NVDA/CRWV/GOOG/DRAM/GLD/UUUU + demo MANUAL "ขายหลักทรัพย์" 260) MOVED 4010→4020; flat STATEMENT "กำไรจากการขายหุ้น" rows (restored baseline) + demo MANUAL "รับเงินปันผล" 150 MOVED 4020→4010 — user 9f5a2c2b: 4010 now 102.66/9 rows, 4020 now 97.10/7 rows; whole-ledger imbalance unchanged (only the 2 known one-cent rounding entries #241/#258). `app/lib/posting-engine.ts`: `GAIN_INCOME` 4010→**4020**, `DIVIDEND_INCOME` 4020→**4010** (+ the two stale "gain -> 4010/4010+5120" comments fixed to 4020) so NEW imports post gains to 4020 and dividends to 4010 — consistent with the swapped data. `scripts/seed-general-ledger-demo.mts` updated to the new layout (SELL gain demo now credits 4020, dividend demo now credits 4010). Tests updated in `scripts/test-general-ledger.mts`: dividend credits 4010, capital-gain section routes to 4020, computable SELL gain credits 4020 (60 asserts, all inverted to the new mapping). npm test EXIT 0 all 16 suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Statement import page now PREVIEWS before committing (approved: "แสดงรายละเอียดการนำเข้าก่อน + ปุ่ม OK เพื่อทำการนำเข้าให้เสร็จสมบูรณ์"; summary + full table; tick-all + OK, no per-row checkboxes). New read-only `POST /api/v1/statements/preview` (`app/routes/api/statements/preview.ts`, registered in app/routes.ts next to upload): verifyAuth → `validatePdfFile` + `hasPdfMagicBytes` (now exported from statement-storage.ts) → content-hash duplicate detection (`computeContentHash`/`findExistingDocumentByHash`) → `extractTextFromPdfBytes` → `buildStatementTransactions` (loads `loadCostBasisState` but NEVER saves) → `applyFxRateFallback` (statement FX wins, cache-first provider) → returns `{ preview:true, fileName, documentId, duplicate, duplicateDecision: "fresh"|"rebuilt"|"unsupported", extracted, rows[ValidatedCapitalRow], rejected, stats }`. STRICTLY read-only: no storage object, no documents row, no Capital_Transactions insert, no cost_basis_state write, no ledger postings, no Gemini, no audit. Duplicate-with-rows → `buildDuplicatePayload`; file with 0 rows → `duplicateDecision:"unsupported"`. `StatementUploadPage.tsx`: new `reviewing`/`review` phases — attach PDF → magic check → preview fetch → "ตรวจสอบเอกสารก่อนนำเข้า" screen (summary cards ที่พบ/จะนำเข้า/BUY/SELL/CASH/SELL คำนวณกำไรได้/อัตราจาก Statement + full server-field table วันที่/รายการ/ฝั่ง/จำนวน/ราคา/หน่วย/มูลค่ารวม/ค่าธรรมเนียม/เงินเข้า-ออก/อัตรา FX/กำไร-ขาดทุน THB, rebuild amber note) → ปุ่ม "OK — นำเข้า N รายการ" fires the UNCHANGED `POST /api/v1/statements/upload` commit path (INSERT + cost basis + backfill + postings) → "ผลการนำเข้า" as before; "ยกเลิก" resets with zero side effects; preview duplicate → existing Statement ซ้ำ modal (no commit); preview 0 rows → error message. No P&L/FX recompute in React (all rows render server fields directly). Tests: test-ui-placeholders.mts +16 (route registered; preview reuses validation/pipeline/hash; strictly read-only — no storage/insert/basis/postings/gemini/audit; duplicate short-circuit; carries full rows+stats; rebuilt-label; hasPdfMagicBytes export; page calls preview endpoint; reviewing/review phases; review screen + table fields; OK commits via original upload; attach only previews first; duplicate/unsupported never reach review) → 106 PASS; run-tests.mts REG: STATEMENT PREVIEW block (gated TEST_DATABASE_URL; real route calls — fresh preview returns ≥3 rows + buyCount 2/sellCount 1 + decision "fresh", DB row/cache/document counts UNCHANGED after preview, real commit after preview saves ≥3, second preview returns duplicate STATEMENT_ALREADY_IMPORTED; FK-safe cleanup). npm test 570 PASS/0 FAIL all 16 suites; typecheck + build clean; git diff --check clean (CRLF notice only). test:w2 REG block NOT run locally (no TEST_DATABASE_URL). Changes NOT committed.

Next:
- Wait for user instruction (pending human steps: Vercel credentials/project link + adapter choice + env vars SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_STORAGE_BUCKET, DATABASE_URL, JWT_SECRET, GEMINI_API_KEY, GEMINI_MODEL in the Vercel dashboard; then deploy preview; then commit/push/PR).

- Webull Average Cost method implemented (approved plan: "start") — the cost basis (and thus SELL realized gain/loss → tax base + GL postings) now uses the Webull **"Average Cost"** definition, FEES EXCLUDED: `avgCost = cumCost / cumQuantity` where `cumQuantity` = lifetime BUY qty (NEVER reduced by SELL) and `cumCost += qty × price` per BUY; a SELL only reduces the live `quantity` (avg unchanged) and **full liquidation drops the entry** (next BUY resets). Divergence from the old running-average: buy 1000@300 → sell 500 → buy 200@350 ⇒ 308.33 (old 314.29). Built a NEW pure shared engine `app/lib/cost-basis-engine.ts` (`CostBasisPosition {quantity, avgCost, cumQuantity, cumCost}`, `applyAverageCostTrade(map, symbol, side, qty, price, seed?)`, `isEmptyPosition`) as the SINGLE source of truth for the statement parser, `recomputeCostBasisMap`, and the backfill/recompute engines (old inline running-average math in statement-pipeline split into the SELL-deduct + BUY-average branches was replaced). `app/lib/corporate-action.ts` now carries the cum fields through SPLIT/REVERSE_SPLIT (scale cum_quantity × ratioNew/ratioOld, cum_cost constant)/RENAME/SPIN_OFF; `app/lib/pdfStatementParser.ts` re-exports the engine types, `loadCostBasis` hydrates legacy rows (cum = qty × avg), `parsePortfolioSummary`/`parseStatementRows` seed + replay via the engine (drained entries deleted, no more phantom zero-qty entries). `app/lib/statement-pipeline.ts`: `loadCostBasisState` reads the new cum columns (+legacy hydration), `saveCostBasisState` writes them via decimalString, `computeGainLossBackfill` + new `recomputeAllGainLoss` (pure FULL recompute that OVERWRITES already-computed AI_PARSED SELLs, never MANUAL) both replay through the engine and share a new `realizedUpdateFor` helper (net = stored net_amount else gross − fees; FX = fx_rate_effective else THB→1; THB from the ROUNDED gain — parser parity). `rebuildCostBasisStateFromLedger` + `backfillComputedGainLoss` anti-regression: corporate_actions table missing on a deployment is now truly tolerated (the "does not exist" signal lives in DrizzleQueryError.cause, not .message — fixed all 3 catch sites). Migration `drizzle/0019_add_cost_basis_cumulative_columns.sql` (`ALTER TABLE cost_basis_state ADD COLUMN cum_quantity numeric NOT NULL DEFAULT '0' / cum_cost numeric NOT NULL DEFAULT '0'`) + `app/db/schema.ts` gains `cumQuantity: numeric("cum_quantity")`/`cumCost: numeric("cum_cost")` (drizzle journal stops at 0013, so applied to the dev DB manually via ALTER ... IF NOT EXISTS, same as 0014–0018). One-shot CLI `scripts/recompute-cost-basis-webull.mts` ([userId] filter + --no-gl): per user replays the full ledger + actions under the Webull engine, OVERWRITES the affected AI_PARSED SELL rows (only where the stored value differs — idempotent), rewrites cost_basis_state via `rebuildCostBasisStateFromLedger`, and (default ON) re-posts the general ledger for changed rows (reverse + `buildStatementPostings` + `insertPostings`, source_transaction_id linked, best-effort). Applied to the dev DB (docker :55432): 0019 columns added; recompute ran clean across all users — totalRecomputed=57 AI_PARSED SELLs / changed=0 (dev history has no buy-after-sell interleave ⇒ old vs Webull averages coincide; verified engine-vs-stored MISMATCH=0 and avg === cumCost/cumQuantity for all 11 holdings of user 9f5a2c2b, e.g. TSLY qty 33 vs cum divisor 63), GL untouched. Tests: test-statement-tax-recon.mts +6 → 54 PASS (Webull 308.33 replay, buy→sell→buy divergence basis 154166.67, full-liquidation reset, recomputeAllGainLoss overwrites-already-computed + skips MANUAL); run-tests.mts gains REG: WEBULL AVERAGE COST RECOMPUTE (gated on cum_quantity; real engine + DB path: OLD-method stored 157142.86 SELL overwritten to Webull basis 154166.67/gain 10833.33/THB 383716.55, MANUAL SELL untouched, cost_basis_state qty 700 + cumQty 1200 + cumCost 370000 + avg 308.33, self-cleaning — NOT run locally, needs TEST_DATABASE_URL). npm test all 16 suites FAIL: 0 (exit 0); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.
- Document viewing improved (approved: "view past transaction records"): a stored Statement can now be opened to show EVERY ledger row that came from it, without downloading. New GET-only (action 405) `GET /api/v1/documents/:id/transactions` (app/routes/api/documents.$id.transactions.ts, registered in app/routes.ts): user-scoped; UUID-validated (400 on malformed); ownership-verified with a safe 404 for missing/cross-user ids; returns the committed Capital_Transactions rows (same raw shape as GET /api/v1/capital-ledgers, ordered transaction_date/transaction_id) + stats {total,buyCount,sellCount,cashCount,computableSellCount,fxRates} derived server-side; purely read-only (no storage read, no re-parse, no writes - the ledger is the source of truth). `GET /api/v1/documents` now returns transactionCount per file (LEFT JOIN aggregate over the caller's OWN source_document_id rows; file_path still never exposed). Frontend: server-api.ts gains `ServerDocumentMeta.transactionCount` (mapped ?? 0), `DocumentTransactionStats`, `DocumentTransactionsResponse`, `fetchDocumentTransactions`, and CapitalLedgerRow gains category/section; StatementArchivePage gains a per-file "ดูธุรกรรม" button (Table2 icon) that expands an inline panel - summary cards (นำเข้าทั้งหมด/BUY/SELL/CASH/SELL คำนวณกำไรได้/อัตราจาก Statement + rates list) + a table mirroring the import-preview columns (วันที่/รายการ/ฝั่ง/จำนวน/ราคา/หน่วย/มูลค่ารวม/ค่าธรรมเนียม/เงินเข้า-ออก/อัตรา FX/กำไร-ขาดทุน THB), rendered from server fields only (no P&L/FX recompute in React); loading / error+ลองใหม่ / honest empty ("เอกสารนี้ไม่มีธุรกรรมในระบบ") states; row subtitle shows "· N รายการ" from transactionCount; panel toggles on re-click and closes on delete; StoredDocumentMeta carries optional transactionCount. No migration, no storage changes, download path untouched. Tests: test-ui-placeholders.mts +16 -> 119 PASS (route registered/user-scoped 404/GET-only, server-stats fields, transactionCount aggregate + no filePath leak, client DTOs, archive button/panel/states/server-field rendering, subtitle count); run-tests.mts gains REG: STATEMENT TRANSACTION VIEW (gated TEST_DATABASE_URL: upload -> own 200 with rows+stats+FX 35.42 listed, cross-user 404, malformed id 400, documents-list transactionCount=3, self-cleaning - NOT run locally). npm test 16 suites FAIL: 0 (ui-placeholders 119); typecheck + build clean; git diff --check clean (CRLF notice only). Verified live on dev DB (user 9f5a2c2b, docker :55432): 2025-12.PDF -> 200 total 33/buy 21/sell 1/computable 0/fx [1,31.1526,31.45,31.5457,31.746], 2025-11.PDF -> 200 total 12/fx [1,32.11,32.4675], cross-user (51a124a0 token) -> 404, documents list shows txCount 33/12/44/39. Changes NOT committed.
- GL transaction-record drill-down done (approved: the per-account movements table — incl. the Revenue -> Dividends page — replaces the "เอกสาร" (statement-download) column with a "ธุรกรรม" column whose "ดูธุรกรรม" button opens the source Capital_Transactions entry for the clicked line). `app/lib/ledger-service.ts` `getAccountLedger` now carries `sourceTransactionId` (from the posted journal entry) onto every line view; `server-api.ts` extends `GeneralLedgerLineView.sourceTransactionId` and adds client `fetchUserTransaction` -> reuse of the EXISTING owner-scoped `GET /api/v1/capital-ledgers/:id` (no new endpoint). `AccountCategoryView.tsx` (LedgerRedesign): column "เอกสาร" -> "ธุรกรรม", per-line button "ดูธุรกรรม" (Table2) when `sourceTransactionId` present (else "-"), removed the old download handler/states; new in-page drill-down "บันทึกธุรกรรม" (back button กลับไปรายการเคลื่อนไหว) renders the record's server fields verbatim — วันที่/หุ้น/ฝั่ง/จำนวน/ราคาต่อหน่วย/ราคารวม/ค่าธรรมเนียม/เงินเข้า-ออก/จำนวนเงิน (สกุล+THB)/อัตราจาก Statement/อัตราที่ใช้จริง/ต้นทุน/กำไร-ขาดทุน (+THB)/หมวด/ส่วน/ตลาด — with loading, error+ลองใหม่, and honest not-found ("ไม่พบบันทึกธุรกรรมนี้ (อาจถูกลบไปแล้ว)"); no P&L/FX recompute in React. Tests: test-ui-placeholders.mts +9 (column rename, ดูธุรกรรม driven by sourceTransactionId, old download gone, fetchUserTransaction + GeneralLedgerLineView.sourceTransactionId, drill-down wiring + states + server-field rendering + no-recompute) -> 127 PASS; run-tests.mts gains REG: TRANSACTION RECORD VIEW (dedicated user; upload -> posted journal line has sourceTransactionId -> GET single tx 200 id+symbol match, cross-user 404, account-ledger route surfaces sourceTransactionId on the line; self-cleaning incl. postings + stored file - NOT run locally). npm test 16 suites FAIL: 0 (ui-placeholders 127); typecheck + build clean; git diff --check clean (CRLF notice only). Verified live on dev DB (docker :55432): 2026-06-30 MLPI journal line -> account-ledger shows sourceTransactionId, GET single tx 200 symbol MLPI, cross-user (51a124a0 token) -> 404. Changes NOT committed.
- Transaction viewing made easier (approved plan: in-page panel + prev/next + category-aware fields). `app/component/LedgerRedesign/AccountCategoryView.tsx`:
  - REMOVED the full-page "บันทึกธุรกรรม" replacement view (no more two-level back) — the drill-down now opens as an in-page `<Panel>` right below the movements table, so the account context/scroll position and the back-to-account flow are preserved.
  - Added prev/next navigation across the account's linked records: `linkedTxIds` (useMemo over `lines` filtered by `sourceTransactionId`, display order) + `txIndex` + `goToTx`; panel header shows "บันทึกธุรกรรม · รายการที่ i/N" and a "ก่อนหน้า/ถัดไป" control row (disabled at bounds); the currently-open row in the movements table is highlighted (`selectedTxId === l.sourceTransactionId` → bg-blue); a "ปิด" (X) button in the panel header closes it.
  - Category-aware field layout via pure `txRecordFields(rec)` builder: renders a summary header (side badge, symbol, date, ซื้อ/ขาย qty × price, section, big จำนวนเงิน (บาท), and กำไร/ขาดทุน (บาท) colored when SELL + computable) + a "รายละเอียดธุรกรรม" list that shows ONLY fields with a value (guards `!= null`) so CASH/income rows don't show trade-only "-"; `<h3>` header + footer "ไม่คำนวณใหม่" kept, still server-authoritative (txRecord.realizedGainLossThb rendered verbatim, no toFixed/times recompute).
  - `selectedTxId`/`txRecord` reset when the account + period reloads (loadDetail), so a stale drill-down can't linger after a filter change.
  Tests: test-ui-placeholders.mts section 12 updated (asserts no longer require the old full-page back string; now require in-page close/prev/next + i/N + detail header + not-found) + new asserts (txRecordFields/sideLabelFor category grouping, row highlight, no full-page view) → 131 PASS. npm test 16 suites FAIL: 0 (ui-placeholders 131); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Transaction viewing made easier — v2 (approved: full-width screen inside the GL tab, replaces the in-page panel). \`app/component/LedgerRedesign/AccountCategoryView.tsx\`: the "ดูธุรกรรม" drill-down is now a dedicated full-width "บันทึกธุรกรรม" screen (early-return \`if (selectedTxId)\` BEFORE the account-view branch, so it replaces the whole GL tab content — no more panel stuffed under the movements table). Breadcrumb "← กลับไป[ชื่อบัญชี]" returns instantly (selected/period/lines stay in component state, no refetch); accent-gradient banner with side badge + "บันทึกธุรกรรม · วันที่" + account/symbol; the prev/next control (รายการที่ i/N + ก่อนหน้า/ถัดไป) moved INTO the banner for quick browsing of the account's linked records; big summary block (ฝั่ง badge, symbol, ซื้อ/ขาย qty × price, section, จำนวนเงิน (บาท) big, กำไร/ขาดทุน (บาท) colored when SELL+computable) + "รายละเอียดธุรกรรม" via category-aware \`txRecordFields(rec)\` (only non-null fields) + footer "ไม่คำนวณใหม่"; loading/error+ลองใหม่/not-found states; row highlight (\`selectedTxId === l.sourceTransactionId\`) retained in the movements table; \`loadDetail\` still resets the drill-down. Tests: test-ui-placeholders.mts section 12 updated (full-screen early-return before the account view, banner prev/next, no \"{selectedTxId && (\" inline panel, no two-level back) -> 132 PASS. npm test 16 suites FAIL: 0 (ui-placeholders 132); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Per-stock detail screen done (approved: "รายละเอียดหุ้นรายตัว" — case-by-case stock checking with per-stock P&L). New user-scoped `GET /api/v1/portfolio/:symbol` (`app/routes/api/portfolio.$symbol.ts`, registered in app/routes.ts next to cost-basis): symbol trim+uppercase + `SYMBOL_REGEX /^[A-Z0-9.\-]{1,12}$/` → 400; ownership-scoped (safe 404 when the caller has NO trades AND no holding for the ticker — never leaks existence cross-user); returns ALL of the user's Capital_Transactions rows for the symbol (ordered transaction_date/transaction_id), the current holding from cost_basis_state (quantity/avgCost/cumQuantity/cumCost + server-computed `totalCost`, plus display-only `marketValue` = qty × latest close and `unrealizedPnl` = marketValue − totalCost), the latest daily close from the global stock_prices table (quote null when absent), and server totals `{tradeCount,buyCount,sellCount,cashCount,computableSellCount,nonComputableSellCount,totalRealizedThb}` where `totalRealizedThb` = Decimal Σ realizedGainLossThb over computable SELLs. Graceful degradation: a deployment WITHOUT the stock_prices table (0017 not migrated) still serves trades/holding/totals — the quote query catch sets quoteRows=[] instead of 500ing (same pattern as the dashboard holdings card). Read-only: action → 405. Frontend: `server-api.ts` gains `PortfolioDetail`/`PortfolioHoldingDetail`/`PortfolioQuoteDetail`/`PortfolioTotalsDetail` + `fetchPortfolioDetail(accessToken, symbol)` (hits `/api/v1/portfolio/${encodeURIComponent(symbol.trim().toUpperCase())}`); new `app/component/DashboardUser/StockDetailPage.tsx` (props `{ symbol, onBack }`) — gradient banner, 3 summary cards (การถือครอง / ราคาและมูลค่าตลาด / กำไร/ขาดทุนที่รับรู้แล้ว with amber "SELL ที่ยังคำนวณไม่ได้ N รายการ" note + totalRealizedThb บาท), full trades table (วันที่/รายการ/ฝั่ง/จำนวน/ราคา/หน่วย/มูลค่ารวม/ค่าธรรมเนียม/เงินเข้า-ออก/อัตรา FX/กำไร-ขาดทุน (THB)), loading/error+ลองใหม่/not-found/empty states, all money rendered verbatim (no P&L/FX recompute in React); `DashboardHomePage.tsx` gains required `onOpenSymbol` prop and the holdings symbol cell is now a clickable button (footnote "คลิกที่สัญลักษณ์เพื่อดูรายละเอียดหุ้นรายตัว"); `Dashboard.tsx` gains `selectedSymbol` state with an early-return `<StockDetailPage symbol onBack/>` branch (replaces the whole tab content, back returns to the home screen). Tests: test-ui-placeholders.mts +13 section -> 147 PASS (route registered, auth/user-scoping/404/400/405, server-computed totals/holding/quote, client DTOs, clickable home holding, Dashboard wiring, table columns, honest states, no Decimal/.times in page); test-server-api.mts +4 per-stock client asserts -> 47 PASS; run-tests.mts gains "REG: PER-STOCK DETAIL (CASE-BY-CASE STOCKS)" (dedicated users; upload 2-BUY-1-SELL VRMAX fixture -> owner 200 with 3 trades + totals (computableSellCount 1, totalRealizedThb = net 1497.93 − basis 750 = 747.93 × 35.42 = 26491.68) + holding 150 @ 15 (total 2250) + optional stock_prices seed -> quote 25.00 / marketValue 3750 / unrealizedPnl 1500 (skipped when the 0017 table is absent, asserting quote null instead); lowercase "vrmax" resolves; cross-user + unknown-owner symbol -> 404; bad/oversized symbol -> 400; POST -> 405; FK-safe cleanup incl. the seeded quote; NOT run locally — needs TEST_DATABASE_URL). npm test EXIT 0 all 16 suites FAIL: 0 (ui-placeholders 147, server-api 47); test:w2 REG block NOT run locally; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- **Journal-as-SSOT Phase 2 COMPLETE** (approved: "start") — consumers now READ the journal; manual cash rows are journaled; the list is a pure 1:1 projection. New read layer `app/lib/journal-ledger-read.ts` (`CapitalJournalRecord` + pure `journalEntryToCapitalRow` (verbatim passthrough; MANUAL→MANUAL else AI_PARSED; type passthrough; fxRateBot→null) + DB helpers `listCapitalLedgerRows`, `getCapitalLedgerRow`, `listCapitalLedgerRowsByDocument` (sourceDocumentId), `listCapitalLedgerRowsBySymbol` (status POSTED), `listCashSummaryRows` (sourceTransactionId NOT NULL AND (category equity OR sourceType MANUAL)) — all filtered source_transaction_id IS NOT NULL so GL-manual journal entries never leak in; ordered entryDate/entryNo). Rewired routes/libs: `app/routes/api/capital-ledgers.ts` GET list; `capital-ledgers.$id.ts` GET single; `app/lib/cash-summary.ts` getCashSummary; `app/routes/api/portfolio.$symbol.ts` trades; `app/routes/api/documents.$id.transactions.ts`. Write-path journal wiring in `app/lib/ledger-service.ts`: `insertManualCashJournal` (desc ฝากเงินเข้าบัญชี/ถอนเงินจากบัญชี, detail category equity + type + money, Dr cash 1010/1020 ↔ Cr owner capital 3010, lazy CoA seed, best-effort CreateEntryResult); `syncCapitalLedgerJournal` (PUT: updates header money/date/currency/type, rebuilds only the 2-leg equity entry, statement multi-leg lines untouched), `removeCapitalLedgerJournal` (DELETE: hard-deletes linked entry+lines, not a reversal). `capital-ledgers.ts` POST mirrors MANUAL rows into the journal + returns additive `journalEntryNo` (AI_PARSED already journaled by import); `capital-ledgers.$id.ts` PUT fires sync for CASH rows, DELETE fires remove (best-effort). Migration 0020 (UNAPPLIED anywhere) + schema.ts extended: `journal_entries.type text` (CASH_IN/CASH_OUT; GL manual UI null) + source_transaction_id index; `insertStatementImport` persists `type`. NOT rewired (kept mirror reads): analysis, export-service, daily-aggregation, stock-price-refresh, cost-basis. Tests: test-general-ledger +7 pure mapper → 80 PASS; test-ui-placeholders +10 (section 14) → 164 PASS; run-tests REG: CASH SUMMARY asserts journal mirror (type/amount/THB/fx + 2 lines) + cleans journal lines→entries→capital rows (DB-backed, NOT run locally). npm test 16 suites FAIL: 0 (617 total); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed. Phase 3 (backfill legacy Capital_Transactions → journal) still pending.

- **Journal-as-SSOT Phase 3 COMPLETE** (approved: "start") — legacy Capital_Transactions rows are backfilled into the journal. New script `scripts/backfill-journal-ssot.mts` (`[userId]` filter + `--dry-run`): per user loads all capital rows + the user's journal entries, and for each row WITHOUT a `source_transaction_id` link does EXACTLY ONE of — `alreadyMirrored` (journal already links it, no-op) / `linkLegacy` (exactly ONE unlinked journal entry matches on entry_date + description → that is the row's own old GL posting; UPDATE it with the link + sourceDocumentId + type + full trade detail via `journalDetailOf`; postings/lines untouched) / `recordOnly` (no match → new SKIPPED line-less mirror entry, status POSTED, skipReason "backfilled-record-only", so reads see the row WITHOUT inventing GL postings — matches import SKIPPED semantics) / `manualCash` (legacy MANUAL CASH_IN/OUT with no posting → inserted as a real 2-leg POSTED equity entry via the existing `insertManualCashJournal`, Dr cash 1010/1020 ↔ Cr owner capital 3010) / `ambiguous` (>1 unlinked match, rare → SKIPPED mirror skipReason "backfilled-ambiguous-legacy", never links the wrong entry). Row→ValidatedCapitalRow reconstruction + `statementDescriptionFor`/`journalDetailOf` reused from posting-engine. `app/lib/ledger-service.ts` gains `insertBackfilledJournalEntry(userId, BackfilledJournalEntryInput)` — SKIPPED line-less record insert (status journal_entries.status POSTED, postingState SKIPPED, `lines: []` — NEVER fabricates posting lines), validated via validateJournalEntry, lazy-free single-entry insert with nextEntryNo. Postings are never invented; the ONLY write to an existing entry is attaching the unambiguous link + detail on legacy postings. Requires migration `drizzle/0020_add_journal_entry_trade_details.sql` (contains all trade-detail columns + `type` + `journal_entries_source_transaction_id_idx`). **LIVE VERIFIED on the dev DB** (docker `stax-dev-db` on localhost:55432, which was running all along — earlier "BLOCKED" note was wrong): applied 0020 (26 columns/indexes) via a guarded idempotent runner; dry-run predicted exactly the real outcome; real run backfilled 188 legacy AI_PARSED rows as recordOnly mirrors across 4 users (9f5a2c2b 48 / 51a124a0 56 / 0ca8cd7e 32 / d8a4e289 52) with failed=0; linkLegacy=0 by design (the only 6 unlinked journal entries are MANUAL demo postings that don't match statement descriptions); no legacy MANUAL cash rows exist so the manualCash branch stays test-covered only. Post-run verification: coverage=1132/1132 (every Capital_Transactions row now has a journal link), mirrors=188 (all postingState SKIPPED, skipReason backfilled-record-only, status POSTED, with trade detail, 0 journal_entry_lines — GL untouched/balanced), per-user read coverage 100% (189/374/315/254 via the journal SELECT), journal_entries 1035→1223, re-run is a no-op (alreadyMirrored=1132). 85 pre-existing journal entries still point at deleted capital rows (orphans, out of scope). Tests: test-ui-placeholders.mts +6 section 15 (script reuses insert helpers + pure builders; links id/document/type; entry_date+description matching + alreadyMirrored; record-only/ambiguous SKIPPED mirrors; dry-run; ledger-service helper is line-less) → 170 PASS; test-general-ledger unchanged 80 PASS. npm test 16 suites FAIL: 0 (623 total); typecheck + build (EXIT 0) clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Journal page (สมุดรายวัน) made easier to read/understand (approved: full set, cards stay expanded by default, UI-only + GL tab untouched, backend not touched — all values still served verbatim from the journal, no P&L/FX recompute in React): `app/component/Journal/JournalPage.tsx` gains 5 comprehension aids — (1) friendly one-line summary per entry `tradeSummary` (BUY/SELL -> "ซื้อ NVDA 10 หุ้น @ 150.00 USD"; deposit/withdraw detected from server description "ฝากเงินเข้า"/"ถอนเงินจาก" -> "ฝากเงินเข้าบัญชี ฿..." with the original description kept as a subtitle when it differs; manual/other rows fall back to the description untouched); (2) Thai labels `friendlyCategory` (asset→สินทรัพย์, equity→ส่วนทุน, income→รายได้, expense→ค่าใช้จ่าย) used in the หมวดหมู่ detail row + an AccountTypeBadge (สินทรัพย์/หนี้สิน/ส่วนทุน/รายได้/ค่าใช้จ่าย, reused from shared.tsx) on every account line; (3) collapsible "วิธีอ่านสมุดรายวัน" legend explaining the colors (เดบิต=เขียว/เครดิต=แดง) and the ลงบัญชีแล้ว vs ข้าม (ไม่ลงบัญชี) difference; (4) live client-side search `matchesSearch` across description/symbol/accountName/accountCode/memo/#entryNo + result count "พบ N รายการ" + honest "ไม่พบรายการที่ค้นหา" state (counters stay period/status-based); (5) grouping by date (`groupEntriesByDate` + Thai day header "14 ก.ย. 2026" + count per day) and per-card expand/collapse (ChevronDown/Up, default fully expanded) with ยุบทั้งหมด/ขยายทั้งหมด buttons and a collapsed "กดเพื่อดูรายละเอียด" row. All existing up-stream behaviors kept: fetchJournal filters (period + sourceType/postingState), SKIPPED skip-reason banner + status filter, reverse flow (reverseJournalEntry/isReversing), ดู Statement + archive empty-state link, the verbatim ข้อมูลจาก Statement detail grid and debit/credit table. Tests: test-ui-placeholders.mts +11 ("12b Journal comprehension" block) → 181 PASS; npm test 16 suites FAIL: 0 (634 total); typecheck + build (EXIT 0) clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Debit/credit colors neutralized (approved: no green/red for debit/credit; neutral gray both sides; keep status badges/account-type colors). All debit/credit money columns across the app changed `text-emerald-600`/`text-red-500` → `text-gray-800` (distinguished by column header + "-" placeholders): `app/component/Journal/JournalPage.tsx` (สมุดรายวัน debit/credit cells), `app/component/Ledger/JournalTab.tsx` (GL สมุดรายวัน), `app/component/Ledger/TrialBalanceTab.tsx` (งบทดลอง rows + totals), `app/component/Ledger/AccountLedgerDetail.tsx` (สมุดบัญชีย่อย; startColorFor running-balance + P&L colors kept — not debit/credit). Legend in JournalPage: `เดบิต (เขียว)`/`เครดิต (แดง)` → plain bold gray `เดบิต`/`เครดิต`. Manual-entry side toggle `JournalEntryModal.tsx`: selected DEBIT/CREDIT → `bg-blue-50 text-blue-900` (brand tone, nested ternary simplified). KEPT per approval: สมดุล/ไม่สมดุล badges, AccountTypeBadge (สินทรัพย์เขียว/ค่าใช้จ่ายแดง), เงินเข้า-ออก + กำไร/ขาดทุน colors (CashFlow/Home/P&L) — not debit/credit features. Tests: test-ui-placeholders.mts +3 (legend no color words, debit/credit cells neutral gray in journal views + trial balance + account ledger, modal blue toggle) → 184 PASS; npm test 16 suites FAIL: 0 (637 total); typecheck + build (EXIT 0) clean; git diff --check clean. Changes NOT committed.

- Currency exchange moved to its own top-of-page view (approved: "เพิ่มปุ่ม view ด้านบน" — easier to reach than scrolling to the page bottom; UI-only in `app/component/DashboardUser/CashFlowPage.tsx`, no backend/schema change). `CashView` gains `"exchange"`; the banner view switcher now shows `ตั้งแต่เปิดบัญชี / รายเดือน / ตัดยอด ณ วันที่ / แลกเปลี่ยนสกุลเงิน`. When `view === "exchange"` the query sends only `withDetail: true` (no month/asOf — all-time), the banner title/subtitle switch to "การแลกเปลี่ยนสกุลเงิน / รายการแลกเปลี่ยนสกุลเงินทั้งหมด (ย้ายเงินสดระหว่างสกุล) — ไม่ใช่รายรับ/รายจ่าย ไม่รวม BUY/SELL", and ONLY the exchange section renders (totals chips per TO currency + full table วันที่/จาก → ถึง/จำนวนจาก/จำนวนถึง/อัตรา/ยอด THB + empty state). The money in/out section renders only when `view !== "exchange"`, so the two sections are mutually exclusive and the page no longer duplicates the exchange block at the bottom. Exchange footnote simplified to "รายการแลกเปลี่ยนสกุลเงินทั้งหมด (ทุกช่วงเวลา)…" (the month/asOf suffix is gone since the exchange view is always all-time). Server fields still rendered verbatim (no P&L/FX recompute in React). Tests: test-ui-placeholders.mts +5 ("18b exchange-as-own-view" block — switcher array + button label, `CashView` union + branch, mutually-exclusive money/exchange sections, banner title/subtitle, all-time footnote with no `${month}` interpolation) → 199 PASS. npm test 16 suites FAIL: 0 (662 total); typecheck + build (EXIT 0) clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Currency exchange display built (approved after the old plan was cancelled: display-only, NO GL/5020 posting; scope "แค่การแลกเปลี่ยนสกุลเงิน" + "ตารางเต็ม + รวมแยกสกุล"; principle: give more than we give in). The statement CURRENCY EXCHANGE block already parsed fromCcy/fromAmt/rate but only the TO side was persisted, so migration `drizzle/0021_add_exchange_from_fields.sql` adds `exchange_from_currency`/`exchange_from_amount`/`exchange_rate` to **both** `Capital_Transactions` and `journal_entries` (nullable; only exchange rows carry values; applied to dev DB). Chain updated end-to-end: `App/db/schema.ts` both tables; `app/lib/pdfStatementParser.ts` `ExtractedTransaction` gains the 3 optional fields + the currency-exchange block sets them; `app/lib/statement-pipeline.ts` `ValidatedCapitalRow` + `mapToCapitalRow` (fromAmount via `decimalString(Math.abs(...))`) + `insertStatementTransactions`; `app/lib/general-ledger.ts` `JournalTradeDetail` + `emptyTradeDetail` + `validateJournalEntry`; `app/lib/posting-engine.ts` `journalDetailOf`; `app/lib/ledger-service.ts` all 4 journal insert sites + the capital insert; `scripts/backfill-journal-ssot.mts` + `scripts/recompute-cost-basis-webull.mts` (field map-through). Read layer: `app/lib/journal-ledger-read.ts` `listFxConversionRows(userId)` (journal rows with `sourceTransactionId IS NOT NULL` AND `isFxConversion=true`, ordered entryDate/entryNo) → pure `buildCashExchangeRows(rows, opts)` (same month/asOf scope as `buildCashSummary`; per-TO-currency Decimal totals; legacy null from-fields kept honest) → `CashSummary.exchanges` + `CashSummary.exchangeTotals` (now required members; `buildCashSummary` returns empty defaults). `app/lib/cash-summary.ts` + `app/lib/server-api.ts` gain `CashSummaryExchangeRow {transactionId, transactionDate, fromCurrency, fromAmount, toCurrency, toAmount, rate, amountThb}` + `CashSummaryExchangeTotal {currency, totalForeign, totalThb}`. UI: `app/component/DashboardUser/CashFlowPage.tsx` gains "การแลกเปลี่ยนสกุลเงิน" section (separate from equity money movements) with per-currency total chips ("รวม THB … (…. THB)") + full table วันที่/จาก → ถึง/จำนวนจาก/จำนวนถึง/อัตรา/จำนวนเงิน (THB) rendering server fields verbatim (no P&L/FX recompute), honest empty state "ยังไม่มีรายการแลกเปลี่ยนสกุลเงิน", footnote clarifies exchange rows are currency transfers, NOT income/expense, and respects the month/asOf scope. Tests: test-cash-summary.mts +10 (builder fields/totals/month+asOf scope/sorting/legacy nulls) → 26 PASS; test-ui-placeholders.mts +10 (section 16: migration, schema both tables, pipeline mapping, listFxConversionRows, cash-summary types+builder, getCashSummary mapping, DTOs, UI block + footnote) → 194 PASS; test-general-ledger.mts baseRow + CapitalJournalRecord fixtures extended. npm test 16 suites FAIL: 0 (657 total); typecheck + build (EXIT 0) clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Currency-exchange direction comparison built (approved idea 1, "exchanged more currency back than we exchanged out"): the exchange view now answers directly whether the user brought MORE money back into THB than they exchanged OUT of THB. New pure CashExchangeDirectionTotals interface + uildExchangeDirectionTotals + mptyExchangeDirectionTotals in pp/lib/cash-summary.ts — sums mountThb per direction, both sides THB so the net is a REAL comparison (never a guessed re-conversion): INTO side = rows with toCurrency "THB", OUT side = rows with fromCurrency "THB"; a required mountThb to contribute; legacy rows without fromCurrency are NOT counted on the out side (never assumed); foreign-to-foreign rows touch neither direction; same month/asOf scope as uildCashExchangeRows. uildCashExchangeRows now also returns xchangeDirectionTotals, and uildCashSummary/getCashSummary wire it through (required CashSummary.exchangeDirectionTotals member). pp/lib/server-api.ts carries CashExchangeDirectionTotals on the CashSummary DTO. CashFlowPage.tsx exchange view renders a "เปรียบเทียบทิศทาง (สกุล → บาท เทียบกับ บาท → สกุล)" summary panel (only when at least one direction has data): แลกกลับเข้าบาท N ครั้ง · รวม X บาท vs แลกออกจากบาท M ครั้ง · รวม Y บาท + net line "→ กลับเข้ามากว่า Z บาท" (emerald) or "→ ออกไปมากกว่า Z บาท" (red, magnitude only via Math.abs). Tests: test-cash-summary.mts +7 (direction mix 63350/45500/net +17850 more-in, more-out case, legacy null not counted, foreign-to-foreign neither side, month scope, buildCashExchangeRows carries direction totals, zeroed defaults) → 32 PASS; test-ui-placeholders.mts +6 (type + builder + wire-in field, into/out sums+counts+netThb, server-api DTO, page reads + labels both directions, moreInThanOut + emerald/red magnitude, render-gate on non-empty) → 205 PASS. npm test EXIT 0 all 16 suites FAIL: 0 (691 total); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.
- Legacy exchange rows no longer show a confusing '- → USD' (user reported ambiguity; approved: keep THB/USD codes): 77 legacy rows in dev DB lack xchange_from_currency (pre-0021 imports only recorded the received side), so CashFlowPage.tsx rendered {fromCurrency ?? "-"} → {to} as - → USD. Changed the from-side fallback to "ไม่ทราบสกุล" (rows with full data still render THB → USD / USD → THB as before) + extended the exchange footnote with "รายการเก่าที่ระบบยังไม่ได้บันทึกสกุลต้นทางจะแสดง 'ไม่ทราบสกุล → ปลายทาง'". No backend/schema change; THB/USD codes kept. Tests: test-ui-placeholders.mts +2 (fallback string present + old ?? "-"}-arrow gone, footnote wording) → 207 PASS; npm test 16 suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.
- Exchange from-side backfilled (approved B — user asked why currency was 'unknown' when statements only show USD/THB; verified the answer: ALL 77 exchange rows in both tables had to=USD + NULL from-fields, so from MUST be THB — not a guess). Investigation caught a counting trap: xchange column in Capital_Transactions is the STOCK exchange (NYSE/NASDAQ), not the FX flag — real FX rows are section='แลกเปลี่ยนสกุลเงิน' (77) matching is_fx_conversion=true journal rows (77). New scripts/backfill-exchange-from-fields.mts ([userId] + --dry-run, per-user transaction): fills from='THB' + from_amount=amount_thb + rate=fx_rate_effective (verified 0 rows violate amount_thb == amount × fx within 0.02; to=THB would get from='USD' only, other currencies skipped with warning). Applied to dev DB: 154 rows (77 capital + 77 journal across 4 users), re-run no-op. Consequence: every exchange now renders THB → USD with full from/rate; direction totals count all 77 on the OUT side (net now real instead of skipping them); the 'ไม่ทราบสกุล' footnote removed (fallback ?? \"ไม่ทราบสกุล\" kept in code as a dead guard). Tests: test-ui-placeholders +3 (backfill targeting/dry-run/consistency-check) → 210 PASS. npm test 16 suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Exchange section readability rebuilt (approved all-3, UI-only in CashFlowPage.tsx, no backend/schema change): (1) table 6 -> 5 columns — redundant "จำนวนเงิน (THB)" column REMOVED (for THB→USD rows amountThb === fromAmount, for USD→THB rows it === toAmount, so nothing lost), headers renamed จาก → ถึง -> ทิศทาง + จำนวนจาก/จำนวนถึง -> จำนวนเงินต้นทาง/ปลายทาง, direction cell now renders from/to as colored badges (THB blue / others gray) with the fromAmount/toAmount cells showing the currency unit inline (4,999.80 THB / 152.15 USD); THB-pinned ?? "ไม่ทราบสกุล" guard kept only as dead code; (2) direction panel text -> 2-column visual (grid 2 white cards เงินกลับเข้าบาท / เงินออกจากบาท with big THB numbers + counts, net verdict centered below) — title simplified to เปรียบเทียบทิศทาง; (3) collapsed วิธีอ่าน toggle (CircleHelp, mirrors the Journal legend pattern) with 3 comprehension bullets (from=paid / to=received, rate is the broker's printed rate verbatim, exchange moves are not income/expense). Tests: direction asserts updated to the new visual labels; +5 asserts (5-column table scoped to the exchange section — money-section จำนวนเงิน (THB)/colSpan={6} usage is correct and kept, exchange-scoped slice anchored on the {/* Currency exchange comment, +6->5 colSpan, inline units, badge rendering, legend toggle) -> 214 PASS. npm test 16 suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Journal skip-reason wording made human-readable (user reported entries showing "Not accounted for: backfilled-record-only"; approved Option B — UI-only, no GL repair, backend untouched). Cause: "backfilled-record-only" is the Phase-3 backfill marker for legacy Capital_Transactions rows that never had a GL posting (SKIPPED, line-less journal mirror; DB values unchanged). Fix in pp/component/Journal/JournalPage.tsx: the skip banner now maps that code to ข้อมูลเก่า — นำเข้าก่อนระบบลงบัญชีอัตโนมัติ ยังไม่มีรายการบัญชีคู่ (other skip reasons pass through unchanged) + a new ข้อมูลเก่า (backfilled) bullet in the วิธีอ่าน legend ("ตัวเลขและรายละเอียดอยู่ครบ แต่ยังไม่มีคู่เดบิต/เครดิต สามารถกดย้อนกลับเพื่อดูรายละเอียดได้"). Tests: SKIPPED assert + legend assert extended to cover the Thai mapping → 214 PASS. npm test all 14 suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Trading journal page built (approved spec: new sidebar item + 10-column daily trade log + per-stock portfolio summary with 历史 avg cost). New user-scoped GET /api/v1/trading-journal (pp/routes/api/trading-journal.ts, registered in app/routes.ts): ?from=&to=&symbol=&side= (ALL optional, 400 on invalid), reads listCapitalLedgerRows (journal-as-SSOT, oldest-first), keeps only trade-relevant rows (BUY/SELL + income/expense — equity deposits/FX/GL-manual excluded), replays the Webull engine via new pure pp/lib/trading-journal-engine.ts (classifyJournalSide: BUY/SELL passthrough, income -> DIVIDEND/INTEREST/GAIN, else OTHER; uildTradingJournalEntries snapshots avgCost BEFORE each trade — first BUY null, SELL keeps the unchanged average), returns {entries, holdings, totals} with per-symbol holdings (qty/avgCost from cost_basis_state + latest close/marketValue/unrealizedPnl + realizedPnlThb from computable SELLs). Frontend: TradingJournalPage.tsx (banner + filters bar date/symbol/side + per-stock holding cards with หุ้นคงเหลือ/ต้นทุนเฉลี่ย/มูลค่าตลาด/ผลตอบแทนยังไม่ขาย/กำไรที่รับรู้แล้ว + 10-column table วัน/เดือน/ปี/ซื้อ-ขาย-ปันผล/ชื่อย่อ/ราคา/ปันผล-ต่อหุ้น(-)/จำนวนหุ้น/จำนวนเงิน/ค่าธรรมเนียม/มูลค่าสุทธิ/ต้นทุนเฉลี่ย-ต่อหุ้น; symbol clicks -> StockDetailPage via onOpenSymbol). Dashboard.tsx: NavId gains "trading", nav item "สมุดบันทึกการซื้อขาย" (BookOpenText) after หน้าหลัก, render branch wires onOpenSymbol->setSelectedSymbol. server-api.ts gains TradingJournal DTOs + fetchTradingJournal. Limitation: เงินปันผล/หุ้น renders "-" (parser stores no per-share dividend field; dev DB has zero dividend rows). Tests: new scripts/test-trading-journal.mts (27 asserts) wired into npm test; test-ui-placeholders +10 (section 13b). npm test 15 suites FAIL: 0 (224 ui-placeholders); typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Trading-journal limitations fixed (approved plan items 5+6+4+8; items 1-3 left by design, item 7 deferred). (5) Realized P&L now counts DIVIDEND + INTEREST income per symbol (amountThb summed into realizedBySymbol via an addRealized helper; SELL logic unchanged, non-computable SELLs still excluded) and the holding-card label is now 'กำไร/รายได้ที่รับรู้แล้ว (THB)'. (6) Currency-exchange rows now appear in the journal: new JournalSide FX (classifyJournalSide/isTradeJournalRow take an isFxConversion flag; FX rows get purple 'แลกเปลี่ยน' badge + from-to direction badges, price=broker rate, quantity=from amount, amount=to amount, avg '-'; never touch the cost basis; side filter gains 'แลกเปลี่ยนสกุลเงิน'; totals gains fxCount shown in the banner). (4) Market value labeled 'มูลค่าตลาด (ราคาปิด)' + holdings footnote that it references daily closing prices, not real-time. (8) Table footnote explains the journal entry date + the FX column mapping. Tests: test-trading-journal.mts +10 (37 PASS); test-ui-placeholders.mts +2 (226 PASS). npm test 15 suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Trading journal full vision built (approved A+B+C; stats at portfolio level; notes in the journal page only). (A) Investor notes: migration drizzle/0022_add_journal_entry_note.sql + schema.ts note:text on journal_entries (applied to dev DB) + CapitalJournalRecord/CAPITAL_JOURNAL_COLUMNS carry note; new owner-scoped PUT/DELETE /api/v1/trading-journal/:transactionId/note (app/routes/api/trading-journal.\.note.ts, registered in app/routes.ts; safe 404 cross-user/missing, 400 on non-string/over-2000-char, loader 405; never written by imports/postings/backfills/reversals); server-api updateJournalNote; TradingJournalPage gains a per-row note button (11th action column, 10 spec columns intact) + inline editor (save/cancel/clear-to-delete, error state) + saved-note sub-row; entries carry note verbatim. (B) Period P&L summary: route computes totals.pnlSummary {realizedGainThb, dividendThb, interestThb, incomeThb, unrealizedThb, combinedThb, computableSellCount, nonComputableSellCount} over the date/symbol scope (side filter ignored so stats always describe the whole period); page shows 4 summary cards + amber non-computable note. (C) Behavior stats: pure buildBehaviorStats (computable SELLs only; winRate, profitFactor null without a loss divisor, best/worst refs, avgHoldingDays from quantity-weighted vintage dates mirroring avg-cost math) wired as totals.behavior; page shows 5 stat cards + honest footnote. Tests: test-trading-journal 49 PASS (+12 behavior/note); test-ui-placeholders 236 PASS (+10); run-tests gains REG: TRADING JOURNAL NOTES (gated on note column; owner set/flow-back/clear/cross-user 404/missing 404/400s/405; NOT run locally). Live-verified on dev DB with a throwaway user (PUT 200, note in GET list, pnlSummary realized 7100/combined 7100, behavior winRate 1 + avgHoldingDays 8, DELETE clears to null; temp user fully removed incl. audit rows). npm test 15 suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Trading journal EMPTY on real data — root-caused + fixed via data backfill (approved plan; user: "ข้อมูลในหน้าสมุดบันทึกการซื้อขายไม่มี"). Diagnosis on dev DB (docker stax-dev-db :55432): journal_entries held 1,217 linked rows but side was NULL on ALL of them — the 835 BUY/SELL trade rows were linked to legacy GL postings created BEFORE migration 0020 added the trade-detail columns, so isTradeJournalRow(side,category,section) filtered out everything and the page showed zero entries (tests passed only because fixtures/new imports carry detail). New one-shot script scripts/backfill-journal-detail.mts ([userId] + --dry-run, idempotent, per-user sequential UPDATEs): for every journal entry with a source_transaction_id link to a still-existing Capital_Transactions row, copies the capital row's trade detail via journalDetailOf (same pure mapper imports use) onto the entry's detail columns (all 22 JournalTradeDetail fields + averageCost/isFxConversion + type-when-null); orphans (link to deleted row) skipped; NEVER touches lines/postings/amounts/dates/descriptions/status/notes. Applied to dev DB: dry-run predicted exactly the real outcome; real run filled 944 rows (alreadyComplete=188 recordOnly mirrors, orphaned=85 fc90cb0b rows whose capital rows were deleted — known orphans, skipped); re-run is a no-op (filled=0). Post-run state: journal side BUY 728 + SELL 107 (was 0/0), per-user trade scope 51a124a0=281 / 9f5a2c2b=234 / d8a4e289=183 / 0ca8cd7e=137, dividends 0 (dev data has no เงินปันผล rows — consistent); GL still balanced (USD dr=cr, diff -0.02 = the 2 known one-cent rounding entries, postings untouched). Read-layer verified via throwaway tsx (listCapitalLedgerRows + isTradeJournalRow + buildTradingJournalEntries): 9f5a2c2b 315 linked -> 234 in-scope entries (206 BUY + 28 SELL, first 2025-11-03 BUY BBAI, last 2026-06-30 BUY MLPI, all with source links); 51a124a0 374 -> 281 (242 BUY + 39 SELL); temp script + leftover live-check-tj3.tmp.mts removed. No app code changed (insertStatementImport already persists detail — only legacy data needed repair); this also restores detail for the other journal-detail readers (per-stock portfolio, document-transactions, ledger drill-down). npm test 16 suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notice only). Changes NOT committed.

- Trading journal P&L-summary + behavior cards REMOVED from the UI (approved: user "ถ้าเกิดลบสรุปกำไร/ขาดทุน และ พฤติกรรมการลงทุน จะส่งผลอะไรไหม" -> chosen "ลบเฉพาะ UI"). UI-only in `app/component/DashboardUser/TradingJournalPage.tsx`: the "สรุปกำไร/ขาดทุน" section (4 PnlCards + amber non-computable note) AND the "พฤติกรรมการลงทุน" section (win rate / profit factor / best / worst / avg-holding-days) deleted, plus the now-dead `PnlCard` component. Page order now Banner (trade counts) -> Filters -> สรุปหุ้นในพอร์ตรายตัว (HoldingCards) -> บันทึกการซื้อขาย (table + notes). No import changes (TrendingUp/TrendingDown still used by HoldingCard, Coins still used by the empty-holdings state). Backend UNTOUCHED on purpose: route still computes `totals.pnlSummary`/`totals.behavior`, server-api DTOs + `buildBehaviorStats` + test-trading-journal.mts (49 PASS) + REG: TRADING JOURNAL NOTES all unchanged — verified no other page/route reads these two fields, and the route-side REG block never asserted them. Tests: the two ui-placeholders asserts replaced by one absence assert (!"สรุปกำไร/ขาดทุน", !pnlSummary., !"พฤติกรรมการลงทุน", !behavior., !PnlCard) -> 235 PASS. Typecheck + npm test + build + diff-check pending rerun at commit time. Changes NOT committed.

- Trading journal price column now shows the trade currency unit (approved: user "ราคาใช้หน่วยเป็นอะไร" -> "เอาแบบนั้นก็ได้ทำให้อ่านได้ง่ายหน่อย"). UI-only in `app/component/DashboardUser/TradingJournalPage.tsx` JournalRow: the "ราคา" cell renders `{fmtNum(e.price)}` + a muted `ml-1 text-[10px] text-gray-400 font-normal` suffix `{e.currency}` (e.g. 150.00 USD) for BUY/SELL rows; non-trade rows stay "-", rows with null currency render the bare number as before. No P&L/FX recompute in React (`e.currency` rides on the existing TradingJournalEntry DTO verbatim — no server/type/route/engine change). The unit shown is the statement trade currency (สกุลที่ซื้อขายจริง; US stocks = USD, clarifies the price is not THB). Tests: test-ui-placeholders.mts +1 (fmtNum(e.price) + {e.currency} + muted unit style) -> 236 PASS. npm test EXIT 0 all suites FAIL: 0; typecheck + build + diff-check clean. Changes NOT committed.

- Trading-journal date-filter avg bug FIXED + summary shows current holdings only + table paginated 20/page (approved plan; user questions: "หุ้นหลายตัวไม่แสดงหุ้นคงเหลือ…เกิดจากอะไร" -> cause = cost_basis_state holds ONLY currently-held positions; fully-sold symbols legitimately show "-" — chosen fix: cards for the 11 currently-held symbols only, like Dashboard; table shortening -> chosen pagination). Route `app/routes/api/trading-journal.ts`: (1) avgCostAtTime replay now runs over the FULL lifetime `tradeRows` and `from/to/symbol` narrow the BUILT entries (`scopedEntries`; `side` last) — fixes 23/206 drifted rows found live on user 9f5a2c2b (positions predating the `from` window reported null/wrong averages, e.g. TSLY BUY showed 37.55 instead of 12.449); `realizedBySymbol`/`pnlSummary` now read `scopedEntries`, `buildBehaviorStats` reads date+symbol-scoped raw `scopeRows`. (2) `holdings` maps only `symbolsInScope` with `holdingBySymbol.has(sym)` — 14 fully-sold dev symbols (e.g. GOOG/JEPQ/UUUU/AGI) drop out of the cards; their trades stay in the table and StockDetailPage shows the full history + realized P&L. Page `TradingJournalPage.tsx`: client-side paging `PAGE_SIZE = 20` (pageRows slice, `safePage` clamps after filter changes, `setPage(1)` reset on filter change), pager row (รายการ A–B จาก N · หน้า X/Y + ก่อนหน้า/ถัดไป, shown only when >20 rows) between table and footnote; footnotes extended ("แสดงเฉพาะหุ้นที่ยังถืออยู่…" / "· แสดง 20 รายการ/หน้า"). No P&L/FX recompute in React (all numbers still server verbatim). Tests: test-trading-journal.mts +2 window-drift proof asserts (windowed first-BUY null vs full 200, windowed SELL 300 vs true lifetime 250) -> 45 PASS; test-ui-placeholders.mts +4 (route markers tradeRows/scopedEntries/e.date/entrySymbolOf/holdingBySymbol.has; pager PAGE_SIZE/slice/หน้า/pager-reset; "แสดงเฉพาะหุ้นที่ยังถืออยู่") -> 240 PASS; run-tests.mts gains gated REG: TRADING JOURNAL SCOPE (writes only its own throwaway rows, self-cleaning + smoke-user cleanup: SCOPX pre-window BUY + in-window BUY/SELL -> unfiltered and ?from=2026-01-01 avgs both 20; SOLDX (no cbs row) in entries but not holdings; HELDX (cbs row) in holdings — NOT run locally, needs TEST_DATABASE_URL). Verified: typecheck EXIT 0; npm test EXIT 0 all 16 suites FAIL: 0 (test-trading-journal 45, ui-placeholders 240); build EXIT 0; git diff --check clean (CRLF notice only). Live repro via the REAL route loader on dev user 9f5a2c2b: entries 234/206 (unfiltered / ?from=2026-01-01), holdings 11/11 (exactly the 11 currently-held symbols in both), date-window avg drift 0 (was 23 before the fix), closed symbols (GOOG/JEPQ/UUUU/AGI/BBAI) present in entries but absent from holdings. Changes NOT committed.

- Accounting formula fixes, all 7 approved items built (user chose: report shows BOTH THB-base + per-currency / keep 2-stage rounding + document / SPIN_OFF FMV pair). Phase 1 (account labels): DEFAULT_CHART_OF_ACCOUNTS names swapped to match the engine (4010 = dividends, 4020 = capital gains; engine GAIN=4020/DIV=4010 unchanged, seed-demo + test asserts already matched), ledger-service dividendsBySymbol 4020->4010, AccountCategoryView 4020->4010. Phase 2 (FX honesty): validateJournalEntry keeps fxRateEffective at full precision (only amounts toFixed(2)) and rejects non-THB lines without a positive rate (THB->1; statement SKIPPED-downgrade path keeps imports alive); capital-ledgers POST 400 on non-positive non-THB fxRateBot; JournalEntryModal requires FX for non-THB lines; seed-demo carries illustrative FX; test-general-ledger +4 asserts (missing-FX rejected, 31.425 kept, THB->"1"). Phase 3 (no cross-currency sums): trialBalance/incomeStatement/balanceSheet gain additive THB-base parallels (row *Thb + total*Thb + balancedThb + totalsByCurrency; old fields kept), ledger-service passes amountThb + THB-only openings, getLedgerSummary gains per-row *Thb + totalsThb grand card (netIncome no longer hardcoded 0.00 in the THB view); TrialBalance/Income/Balance tabs render native + muted ฿ sub-lines with THB-base total rows (net-income card now reads netIncomeThb — it previously displayed the naive cross-currency sum as Baht), DashboardHomePage + OverviewTab gain a "total in Baht" card + per-card ฿ sub-lines; pure multi-currency asserts +13, REG-GL THB asserts, ui-placeholders +2. Phase 4 (rounding): 2-stage rounding documented as INTENTIONAL in pipeline/parser/engine comments + test header, penny cases locked (10.005->10.01->354.55 vs single-stage 354.38; THB pin). Phase 5 (SPIN_OFF FMV): migration drizzle/0023_add_spin_off_fmv.sql (nullable parent/child FMV) + schema.ts; corporate-action.ts splits parent cumCost pro-rata by total FMV (total preserved, qty unchanged) with legacy fallback + both-or-neither throw; service validates + persists; route parses + GET derives needsReview; server-api DTOs; pipeline SELECTs x2 + recompute script carry the columns (missing-column deployments degrade to legacy replay); pure FMV tests +6, REG: SPIN_OFF FMV block (gated on 0023; 422/201/needsReview true-false; self-cleaning). Migration 0023 NOT applied anywhere (local .env DB has no corporate_actions table — not the data dev DB; apply with release). Verified: npm test 15 suites 763 PASS / 0 FAIL (was 734: GL 80->97, tax-recon 54->63, ui-placeholders 240->243); typecheck + build clean; git diff --check clean (tracked files; phase targets are untracked). Changes NOT committed.

- Dead finance code removed (approved: delete everything not in the current nav, keep only the archive fix). Deleted 8 files: routes api/export, api/analysis, api/aggregation/daily (+ empty folder), lib export-service, lib daily-aggregation, lib gemini-analysis, component Ledger/CapitalLedgerPage (orphaned from nav — verified zero imports), scripts/test-gemini-insight. Edited: routes.ts (dropped 3 registrations), server-api.ts (removed dead fetchAiAnalysis/exportUserTransactions + AiInsight/AiAnalysisResponse types; all other ledger helpers still have live callers), package.json (dropped test-gemini-insight from npm test + removed xlsx dep — sole consumer was export-service; lockfile re-synced via npm install), run-tests.mts (removed exportRoute/analysisRoute imports + W2-11 export/analysis blocks, kept notifications/FX/users.created_at, renumbered). Kept (still used): gemini-statement-parser, gemini-analysis-storage, notifyAnalysisComplete, GEMINI audit codes. Fixed (kept features): GET /api/v1/documents transactionCount now counts journal_entries.sourceTransactionId (was leftJoin Capital_Transactions) so the archive subtitle agrees with the "ดูธุรกรรม" drill-down incl. SKIPPED mirrors/orphans — UI untouched (same shape); PUT /api/v1/capital-ledgers/:id now re-reads via getCapitalLedgerRow + journalEntryToCapitalRow with the raw capital row only as fallback. Verified: npm test 14 suites 749 PASS / 0 FAIL (was 15/763 — only the removed insight suite is gone); typecheck + build clean; git diff --check clean (CRLF notices only). test:w2 needs TEST_DATABASE_URL (REG blocks edited, not run). Changes NOT committed.

- Client-side search added to every feature that lacked one (approved: fix + fill missing, frontend filter only, no backend change). Verified first that all PeriodFilter "ค้นหา" buttons (Trial/Income/Balance/JournalTab/AccountLedgerDetail/JournalPage) are correctly wired to server fetch — no dead buttons found, so the work was purely filling gaps with the JournalPage `matchesSearch` / Admin `userSearch` pattern (query state + live `.filter()` + "พบ N จาก M" count + honest "ไม่พบ…" empty + ล้าง button; summary counters still count the full loaded set): `StatementArchivePage.tsx` gains a filename search (filters before month-folder grouping, auto-expand kept, empty becomes "ไม่พบไฟล์ที่ค้นหา"); `ChartOfAccountsTab.tsx` + `AccountCategoryView.tsx` account lists gain code/name/type/currency search; `JournalTab.tsx` gains the same `matchesSearch` text search as `JournalPage` (description/symbol/accounts/memo/entryNo parity); `AccountLedgerDetail.tsx` gains description/entryNo/memo search with the count in the Panel header (running balance untouched); `Admindashboard.tsx` audit tab gains one shared `auditSearch` box filtering both upload history (fileName/uploader/status) and access log (user/action/device), with the "show all" gates switched to filtered lengths. TradingJournal + CashFlow filters already work server-side (exact-match symbol + month/asOf scope) — left unchanged to avoid redundant inputs. Tests: test-ui-placeholders.mts +6 section 17 → 266 PASS. npm test all suites FAIL: 0; typecheck + build clean; git diff --check clean (CRLF notices only). Changes NOT committed.
